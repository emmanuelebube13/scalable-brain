# 2026-08-14 — open items after the champion removal

**Status of the system in one line:** System 1 has **zero qualified strategies**. That is
correct and deliberate (FIX-S1-014), and it is milestone **M1 — honest zero**. The live
pointer in GCS has **not** caught up and still serves the disqualified strategy.

This file is the working register for what is left. It is meant to be picked up cold:
each item states what is wrong, what "done" looks like, and how to verify it. Update the
status column in place; do not delete items — mark them DONE with a date so the record of
what was fixed stays readable.

Related: `docs/proposed-fixes/system-1/` (the fix register), `task/2026-W32/wave2/STATE.md`
(the strategy-fleet ledger), `othersystemcommunication/S1-NOTICE-2026-08-14.md` (what
Computers 2 and 3 have been told).

---

## Where things stood at the end of 2026-08-14

Done and pushed to `main` (`5d92828` … `11e1134`):

| | |
|---|---|
| Repo protected | 12 days of Wave-1/Wave-2 work committed in 6 coherent groups; it existed only on this disk before |
| Suite green | **571 passed**; `pytest src/system1` works again (root `conftest.py` — it had been failing with 27 collection errors and only worked via `python -m pytest`) |
| FIX-S1-014 landed | `INTEGRITY_DISQUALIFIED` in `vet.py`, checked before the gates, counted as `integrity_fail`; map is empty, weights `{}` |
| FIX-S1-012 / 013 landed | regime labels by threshold not rank; `CAUSAL_SMOOTHING = True`; re-fit run completed — **all three granularities fitted HMM** (D1 no longer falls back to K-Means), H1 acc 0.964 / kappa 0.942 |
| Re-measured on the new labels | attribution + vetting re-ran log-only: **still 0 qualifying**, `integrity_fail: 8`. Verdict held |
| Computers 2 and 3 notified | `S1-NOTICE-2026-08-14.md` written and uploaded to `gs://scalable-brain-artifacts/handoff/` |

---

## P0 — the live pointer is still wrong

Downstream is reading a model set whose entire map is a strategy that cannot fire in real
time. Until these three items are done, the artefact contradicts the repo.

### P0-1 · Promote the refreshed empty map — `TODO`

The 2026-08-15 00:32Z attribution + vetting run was **log-only**, so `results/state/` still
carries the 2026-08-14 10:50Z artefacts built on the pre-re-fit labels.

- **Do:** `python -m src.system1.vetting.vet --live`, then commit the refreshed
  `regime_strategy_map.json`, `strategy_weights.json` and the vetting report.
- **Done when:** the live map's `generated_at_utc` postdates the regime re-fit, `regimes`
  is `{}` and `rejection_summary.integrity_fail` is present.
- **Risk:** none. The verdict cannot change — integrity is checked before metrics.

### P0-2 · FIX-S1-015, the withdrawal path — `IN PROGRESS`

System 1 can move the live pointer *forward* to a better model set but has no way to say
"there is no model". `non_empty_map` correctly refuses to promote an empty bundle, and no
`withdraw`/`revoke` operation exists anywhere in `src/system1/` or `src/common/storage/`.
So the honest empty result physically cannot reach the consumers.

- **Started:** the design is written into the module docstring of
  `src/system1/serializer/publish_model_set.py` (uncommitted working tree).
- **Still to write:** `withdraw()` — a manifest with `status="withdrawn"`, empty
  `artifacts`, a **mandatory** human reason; archives the superseded manifest to
  `previous_model_set.json` so reinstating is a normal `publish()`; a second withdrawal is
  a no-op specifically so it cannot overwrite the rollback breadcrumb. Plus the
  `--withdraw --reason` CLI, guard tests, and the fix doc.
- **Deliberately not automated:** the orchestrator must never call it. A retrain deciding
  on its own to blank the live model is the single-flag failure mode Computer 2 objected
  to on 2026-08-02.
- **Done when:** tests cover withdraw → archive → reinstate and the double-withdraw no-op,
  and `pytest src/system1` is green.

### P0-3 · Flip the pointer — `BLOCKED — decision`

Building the mechanism is authorised; **using** it against the shared bucket is not yet.
`S1-NOTICE-2026-08-14.md` §2 offered Computer 2 the chance to agree the status-flag schema
before anything is written. Either they reply, or the owner says go.

- **Done when:** `gs://scalable-brain-artifacts/latest.json` reports `status: withdrawn`,
  `previous_model_set.json` holds `2026-07-26T00-27-51Z-b48f48d3_gk-656f09e2`, and the new
  `model_set_id` has been sent to Computers 2 and 3.

### P0-4 · Republish the analytics bundle empty — `TODO`

`system1/analytics/latest.json` → `2026-08-02T00-28-54Z-6f291067` exports OOS per-trade
r-multiple series **per qualified cell**, and every qualified cell was strategy 10. The
TELEM-002 strategy profile and the SIM-001 Monte-Carlo bands are therefore drawn from
look-ahead returns.

- **Do:** regenerate from the empty qualified set and publish, with the reason in the manifest.
- **Watch for:** whether SIM-001/TELEM-002 fall back to a cached bundle when the new one is
  empty. A silent fallback would let the contaminated numbers survive the republish — this
  is the one question the notice asks Computer 2 to answer.

---

## P1 — make the research verdicts trustworthy

Zero of 18 strategies qualify, but the measurement that produced that is not yet the
faithful one.

### P1-1 · Faithful H1-resolution re-run — `TODO`

Every verdict on record comes from `v2_harness --all --no-h1`, which resolves fills on
native bars. When a bar's range contains both stop and target, native resolution must
guess, and the guess favours the strategy. `RISK_AUDIT.json` marks `intrabar` as
`NOT_TESTED` for all of them.

- **Do:** re-run without `--no-h1`, refresh `RISK_AUDIT.json`, and update
  `task/2026-W32/wave2/STATE.md`.
- **Expect:** worse, not better. Nothing should be read as a verdict until this is done.

### P1-2 · `daily_fib_retracement` — `BLOCKED — decision`

Its §7 splits 0.5 take-profit + 0.5 trailing, which is genuinely fractional trailing and
remains unimplemented in `position_engine.py`. It reports 0 trades and therefore has no
verdict at all — it is not failing, it is unmeasured.

- **Owner's ruling needed:** implement partial trailing in the engine, or amend the spec to
  a whole-position trail (a documented deviation, not a silent one).

### P1-3 · `h4_box_breakout` — `TODO`

Its declared pairs were never ingested, so the real-data `assert_no_lookahead_v2` probe
SKIPped. Its "clean" status is unproven, and it reports 0 trades for the same reason.

- **Do:** ingest the declared pairs, or mark the strategy explicitly unmeasurable in the
  fleet ledger. Either is acceptable; leaving it looking measured is not.

### P1-4 · Causal rebuild of `Range_Stochastic_Divergence` — `TODO`

Not abandoned. The audit showed the *implementation* reads the future, not that the *idea*
is worthless — the divergence logic has never been evaluated honestly.

- **Do:** reimplement on `causal_structure.confirmed_swing_points` as an ordinary
  contract-v2 research candidate with **no special standing**; it clears the gates on its
  own merits like the rest.
- **State the likely outcome up front:** a confirmation lag of several bars on a
  mean-reversion entry may remove the edge entirely. That is a legitimate finding, not a
  failure, and it must not be tuned away.

---

## P2 — standing gaps, not urgent today

| | Item | Note |
|---|---|---|
| P2-1 | 30 of 47 buildable strategies never started | Assigned elsewhere by the owner; Wave 2 was interrupted by four API session limits |
| P2-2 | Retrain cron stays **disabled** | Off since 2026-08-02 at Computer 2's request. Second reason now: nothing should auto-publish while the map is empty and unwithdrawable. Re-enable only on their explicit request |
| P2-3 | Gatekeeper rebuild (MODEL-006) | Held permanently until rebuilt — it carried 96.78% of its weight on `strategy_id`, so it filters nothing |
| P2-4 | Pub/Sub transport | No longer the critical path: with zero qualified strategies, an empty queue and no queue carry the same information |
| P2-5 | Repo-wide `black` drift | ~35 files would reformat, almost all pre-existing. Worth one deliberate formatting commit rather than mixing it into feature diffs |
| P2-6 | `archieved/OtherSystems/` untracked | Gitignored — carries a nested `.git` that would land as an unresolvable gitlink. Revisit if it should be a submodule |

---

## Verification — the four commands that tell you where you are

```bash
cd /home/emmanuel/Documents/Scalable_Brain/scalable-brain
source /home/emmanuel/Documents/Scalable_Brain/.venv/bin/activate

pytest src/system1 src/layer0 -q                      # expect 571 passed
python -c "import json;d=json.load(open('results/state/regime_strategy_map.json'));print(d['generated_at_utc'],d['regimes'],d['rejection_summary'])"
python -m src.system1.serializer.publish_model_set --dry-run     # what the live pointer would say
python -m src.layer0.strategies.v2_harness --list                # the research fleet
```

The live pointer itself is `gs://scalable-brain-artifacts/latest.json`. Read it, don't
assume `model-artifacts/latest.json` — the local copy is stale by design.
