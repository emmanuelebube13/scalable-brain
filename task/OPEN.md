# OPEN — the open-items register

**Update this file in place. Do not start a competing list.**
Each item names its evidence. Close an item by deleting its row and recording the outcome
where the evidence lives (a FIX doc, a worklog entry, a comms message).

---

**Rebuilt 2026-08-29.** The previous revision had been truncated to 0 bytes in the working
tree, so nothing was tracking these items. Its committed content was a 2026-08-14 session
log carrying an open-items section — recoverable with `git show HEAD:task/OPEN.md`, and its
narrative substance already lives in `docs/worklog/2026-08-14.md`. The still-open items from
its §3 are carried forward below (O-8 … O-13); the items it marked DONE are dropped.

> One claim from that file was **wrong and had propagated into `docs/critical/REPO_STATE.md`**:
> that `persist_all` is "`DELETE`-then-rebuild with no transaction, so snapshot first" with a
> "default 5 that silently discards half the history". It is `INSERT … ON CONFLICT DO UPDATE`
> — it never deletes — and the default is `--lookback-years 10`. Both corrected 2026-08-29.
> The belief that the writer was dangerous to run is part of why it was only ever run by hand.

---

## Blocking

| # | Item | Evidence | Owner |
|---|---|---|---|
| O-2 | **Re-vet and republish the map on fresh evidence.** The live map (`generated_at_utc 2026-08-24T10:20:53Z`, `qualification_run_id 7fde532c…`) was vetted on trades stopping 2026-08-14. Owner elected to leave it live and re-vet rather than withdraw. The table is now current; this can proceed. Must land before O-5. Use the `run-vetting` skill. | FIX-S1-017 §5 | owner |
| O-15 | **Confirm the first unattended cron run.** Installed 2026-08-29; first firing is Tue 2026-09-01 02:00 UTC (Sun/Mon are not in `2-6`). Check `logs/cron_persist_outcomes.log` and that `last_healthy_run_at` in the writer state advances on its own. Every run so far has been hand-started. | `crontab -l`; FIX-S1-017 §1 | owner |

## Correctness

| # | Item | Evidence | Owner |
|---|---|---|---|
| O-3 | **12 of 67 strategies fail to instantiate.** 9 × `*_RA` import `src.regime_aware`, **removed on purpose after the R3 trial concluded** (see O-9) — stale `dim_strategy` rows to deactivate, not code to restore. 3 × `Range_Bollinger_*` are active in the registry but absent from `get_all_strategies()`. | `results/state/outcomes_writer_state.json` → `failed_instantiate` | owner |
| O-4 | **17,583 orphaned rows** for strategy_ids 7/8/9 that no rebuild reproduces. They still feed attribution and vetting — the FIX-S1-013 shape by a different route. Removable with `python -m src.outcomes.persist_all --reconcile` (destructive, owner-gated). None are in the live map today; that is luck, not design. **Now priced (2026-09-05):** the OOS subset is **12,162 trades = 18.6%** of the OOS bank and **12 of the 209** cells in the latest attribution run. Bank mean R is −0.0701 with them and **−0.0776 without** — every bank-level figure in both engine-validation passes, including the −0.070 the investigation was launched to explain, is **11% composed of code that no longer loads**. It is also the sole cause of the v1 replay-fidelity gap. Argues for doing the reconcile. | FIX-S1-017 §3; `audit/reports/engine_validation_2/report.md` §N1 | owner |
| O-16 | **The gatekeeper applies no threshold at inference — the live gate is inert.** `Scorer.score()` returns only `{status, score}` and never loads the manifest; `run.py` stamps a hardcoded `0.5`, below every calibrated cutoff (0.60–0.80), and that placeholder goes on the wire as `threshold_applied`. No below-threshold refusal exists, so the runtime approval rate is **undefined, not zero**. **Scoring itself works** — measured 2026-08-30, 15/15 (pair × granularity), all 8 live-map strategies known to the model; the earlier "nothing scores live" claim came from a stale comment and is retracted. **DECISION NEEDED BEFORE ANY FIX:** live scores cluster 0.42–0.46 against cutoffs of 0.60–0.80, so switching the gate on refuses **14 of the 15 live map cells** (~93% fewer signals). That is the calibration working as designed (`shipped_approval_rate` 0.079), not a bug — but it is a trading-activity decision, not a code change. **DECIDED 2026-08-30: shadow mode, activation declined.** Routing stays permissive; every row records `model_score`, `threshold_calibrated`, `threshold_regime_key` and `shadow_verdict`. Review after 7 days of live data (from the 2026-08-30 21:00Z open), together with O-17 — the thresholds come from a manifest describing a model Systems 2/3 have never seen, so the shadow rate must be read against that first. **Do not activate as a bug fix.** | `docs/proposed-fixes/system-1/FIX-S1-018-*.md` §Measurement | owner |
| O-17 | **The live scorer runs an unpublished champion.** `Scorer` loads the `models/` directory, not a pointer, so `champion_model.pkl` (`23b6d4ca…`, written 2026-08-24T01:30Z, never published) is not the artifact the wire claims — `bundle_id` names `gk-d614163c` (`8845b442…`). The 2026-08-24T10:26 retrain did not promote (`oos_uplift_ok` failed), so this is drift, not a promotion. The ledger records `gatekeeper_model_sha256` beside `bundle_id` so it is now visible per signal. | `models/champion_manifest.json` vs `models/gatekeeper/latest.json`; FIX-S1-018 §Related | owner |
| O-20 | **The ledger does not record what System 1 decided not to BUILD.** `build_signals` discards candidates at ~12 points before the ledger sees them — no structural regime, no stop declared, no take-profit, **no ATR available**, undecodable direction, plus a bare `except` per (bar × strategy). `signal_id` is minted at `build.py:429`, after all of them, so none leave a row. The no-ATR path is the one `build.py:186-191` records as having once dropped 100% of signals silently, and it is still unledgered. Closing it means minting the id earlier in `build_signals`. **Until then, absence of a ledger row is not evidence a candidate never existed.** Found by the 2026-08-30 audit; scope of the ledger is now stated honestly in its docstring. | `src/signals/ledger.py` docstring; `src/signals/build.py:241-477` | owner |
| O-30 | **`check_cell_degeneracy` can no longer tell its defect from correct behaviour — and it is what blocks the retrain.** With trade geometry in the basis and `strategy_id` out, the dry-run refuses at **73.7%** (42 of 57 cells) against the 50% limit, while carrying a significant OOS uplift (0.0562, p=0.0001) and passing every other gate. The guard infers "the model is discriminating on strategy identity" from bimodal per-cell approval — sound when `strategy_id` was the only per-strategy-constant feature, false now: **13 of 23 strategies have an R:R coefficient of variation below 0.10 and six have sd exactly 0.000**, so a model keying on geometry *must* score near-constant within them. The refusal's own worst cells are `Trend_Donchian_H1`, `Trend_EMA_ADX_H1` and `adx_trend_pullback_ea` — the three with the least geometric variation, none of them identifiable by the model. Guard untouched (threshold, inputs, verdict); only its message was corrected, which had been asserting a cause that is now impossible. **Owner decision: accept and stay blocked / re-scope the guard to degeneracy residual to declared geometry / raise the threshold (worst — discards the evidence rather than correcting the inference).** | `audit/reports/gatekeeper_feature_basis.md` §2.5 | owner |
| O-31 | **The gatekeeper's training target disagrees with the gate it is scored on.** `is_winner` (`r_multiple > 0`) is a **win-rate** target; `oos_uplift_ok` is a **mean-R** gate. Trade geometry moves the two in opposite directions — win rate falls 64%→27% across R:R quintiles — so the model's strongest feature optimises a proxy that contradicts the metric. Measured: the gate approves R:R ~0.4–1.2 and rejects ~1.9–2.0, approved win rate 0.49–0.71 vs rejected 0.31–0.35, **but approved mean R beats rejected in only 4 of 5 OOS folds** (fold 3: −0.0671 vs −0.0584), which the pooled p=0.0001 hides. Not leakage — every input is known at entry — and not a pure tautology either, since the lowest R:R quintile has the *worst* mean R (−0.1302) yet the model still beats the rejected set. **Raised as the evidence WO-04B asked for before any target redefinition; nothing changed.** Needs `measurement-reviewer` + `devils-advocate`. Consider before O-30 — it may change the right answer to it. | `audit/reports/gatekeeper_feature_basis.md` §2.6 | owner |
| O-32 | **A 2.0.0 gatekeeper bundle cannot be scored live yet.** `Scorer` validates against `preprocessor.feature_names_in_` from the shipped artifact, so a 2.0.0 champion would demand `atr_sl_multiplier`, `atr_tp_multiplier` and `entry_signal_type` at inference and refuse every signal with `MISSING_FEATURE`. The live signal already carries everything they derive from (`build.py:525-528` — `atr`, `proposed_entry`, `proposed_sl`, `proposed_tp`); only the mapping is missing. **One trap:** `build.py:500-509` computes `rr_ratio` as *signed* reward/risk returning `None` on an inverted range, while training uses unsigned ATR magnitudes — so the two disagree exactly on the pathological trades (the O-24 shape). The mapping must adopt the training definition. **Prerequisite for promotion, not a live defect** — nothing is promoted and the live 1.0.0 champion is unaffected (verified: still scores 0.4285). | `audit/reports/gatekeeper_feature_basis.md` §"Prerequisite" | owner |
| O-21 | **Ledger rows can outlive their counters on a mid-run crash.** **Detection half CLOSED 2026-09-11** — `src/signals/reconcile.py` reconciles `results/signals/*.ndjson` against `signal_emitter_state.json`, and `heartbeat.check_emitter_counters` consumes it (CRITICAL on a self-contradicting file, WARN on a counter below its ledger floor). Both divergences this item names are now visible as shortfall/surplus rather than invisible. The emission behaviour that causes them is unchanged and this item stays open for that. Original text follows. `run.py` writes ledger rows per granularity as it goes, but `record_emitter_state` is called once at the end with no `try/finally`. If `get_new_closed_bars("H4")` raises after H1 wrote rows, the process dies and no counter ever sees them. Also unreconciled by design: a failed ledger append still increments the tally (owner decision — emission is never blocked), so row count and counters diverge in opposite directions for different reasons. Both are detectable by reconciling `results/signals/*.ndjson` against `signal_emitter_state.json`; neither is currently alarmed. | 2026-08-30 audit, claims 1c/2a/2b | — |
| O-22 | **Publish `dlq_count` into `s1_health.json` — promised in a sent message.** Under Pub/Sub `dead_letter()` publishes nothing to `scored_signal_dlq`; it increments an in-process counter and logs one ERROR line locally (`src/common/queue/pubsub.py:43-46`). So Systems 2/3 currently **cannot** tell a lost wire message from a dead-letter, and `TO-SYSTEM2-3-2026-08-30-*.md` §5 tells them so and commits us to fixing it. Until then they have been asked not to build automated wire-drop alerting. A commitment in a frozen message; closing it is not optional. | the sent message §5; `src/common/queue/pubsub.py:43-46` | owner |
| O-23 | **The same `signal_id` is published twice with a different entry price.** `4af8a6fe…` went on the wire at 2026-09-02T14:15:17Z (entry 158.568) and again at 17:15:27Z (entry 158.849). Three causes confirmed and kept separate: **(a)** idempotency key includes per-run `score_run_id` — cannot match a prior publish — **fix blocked on Q1 (owner)**; **(b) FIXED 2026-09-03** — `deduped_count` was structurally always 0 on Pub/Sub; `QueueBackend.reports_depth_accurately()` added, PubSub returns False, `deduped_count` now reports None on Pub/Sub and integer on LocalDurable — commit `acaa71d`, branch `fix/signal-emission-defects-d6-d7-d8`; **(c) MECHANISM CORRECTED 2026-09-03 — the earlier `PUBLISH_NACK` / watcher-rollback story is FALSIFIED.** The 14:15Z run logged `published_count: 1, dlq_count: 0`, so `watcher.commit()` **was** reached; there is not one `PUBLISH_NACK`/`dead_letter`/`rollback` anywhere in `logs/cron_hourly_signals.log`, and `dlq_count_total` is 0 — no dead-letter has ever occurred. The bar-revision sub-theory is falsified too: the 13:00Z USD_JPY H1 bar closed at **158.568** with a single `ingested_at_utc` and was never revised. The **16:00Z** bar closed at **158.849** — the second emission's entry exactly. So the real mechanism is the original one: the signal is **built from the watcher's newly-closed bar but stamped with the strategy's signal bar** (`build.py:429,438` mint the uuid5 from the strategy's `bar_ts`; `build.py:356-360` take entry from the evaluated row). Entry and levels therefore come from **different bars in the same message**, and the stale `bar_ts` regenerates the same uuid5 every run until a newer signal appears. Confirmed independently by Systems 2/3, who note the second copy still asserts `signal_time_utc: 13:00Z`, so a consumer keying on that field **cannot see the drift at all**. **Fix blocked on Q1 (owner).** See `task/2026-September-week1/signal-emission-defects/FINDINGS-D6.md`. **Owner Q1:** is re-affirmation ever wanted? | same as before + FINDINGS-D6.md | owner |
| O-24 | **A 0.06:1 risk/reward signal reached the wire.** `b97120fc…`, 2026-09-01T17:15:30Z, EUR_USD H4 short, `liquidity_grab_fade` (30): entry 1.15856, SL 1.16286 (43.0 pips risk), TP 1.158295 (2.6 pips reward). **Two causes confirmed (2026-09-03):** (a) strategy 30 has no minimum R:R floor — `max(valid_lows)` resolves to nearly at-the-money when a fresh swing low forms on the signal bar; (b) `confirmed_lows_list` is appended BEFORE order logic at each bar, feeding bar `i`'s own swing into bar `i`'s TP selection (leakage). `forex-strategist`: definitively broken R:R. `leakage-hunter`: SUSPECTED_LEAKAGE. Fixing strategy 30 or disqualifying it takes live output to **0 qualified** cells — owner-visible change. **Fix blocked on Q2 (owner).** See `task/2026-September-week1/signal-emission-defects/FINDINGS-D7.md`. | `results/signals/2026-09-01.ndjson` row 4; FINDINGS-D7.md | owner |
| O-26 | **Strategy 58 re-fires on consecutive bars and System 3 sized both — double exposure, realised.** `d3f01f97` (09-01T15:15Z) and `637f27ac` (09-01T17:15Z) are USD_CAD H1 **long**, two hours apart, entries 1.39044 and 1.39054 — **1 pip apart** — with the same stop. Both passed System 3's gates, both became live orders (2651: 16,081 units; 2659: 15,706 units), and **both were stopped out** (−10.77 and −83.56 CAD). **Distinct `signal_id`s on distinct bars, so this is NOT O-23 and dedupe on `signal_id` would not have caught it.** It is one trade idea sized twice. Together with `0c2d3286` (−70.19) these are the three losses that tipped `consecutive_losses` to 5 and fired System 3's circuit breaker on 2026-09-02 14:50:47 — **still open, `reset_at` null**. Flagged as a risk in the 2026-09-03 audit before it was known to have filled; now confirmed realised. **Open question: should System 1 suppress a same-pair same-direction re-fire within N bars, or is that System 3's exposure concern?** Neither side currently does it. | `docs/comms/replies/S2-REPLY-2026-09-03-*.md` §1; `results/signals/2026-09-01.ndjson` rows 2–3 | owner |
| O-27 | **`attribute.py` now requires an engine version and the value is deliberately unset — a retrain will raise until an owner sets it.** `_load_trades`/`run` take a required `engine_version`; `orchestrator._default_pipeline` reads `attribute.AUTHORITATIVE_ENGINE_FOR_VETTING`, which is `None`. Reason: `fact_trade_outcomes` holds two engines whose `r_multiple` is **not the same quantity** — v2 moves **18–34%** of its stops (breakeven/trailing, 100% favourably in 8,000+ observed moves) and scales out fractionally; v1 moves 0% and its median trade is a full −1.0 stop-out. And **no strategy runs under both engines**, so there is no data-driven tie-break. Three read-only callers (`rank_all`, `designate`, `publish_strategy_stats`) pass the named `attribute.POOLED` opt-in, which logs a WARNING. The retrain cron is not installed, so nothing fires unattended. **Owner decision:** set it to `position_engine_v2` (fill timing favours it; 170 of 209 cells) or `backtest_engine_v1`, and record why. | `audit/reports/engine_validation_2/report.md` §B2; `issues/September-Week-1/2026-09-05.md` | owner |
| O-28 | **A pip-scaling defect gives four research strategies stops 24×–47× too tight on USD_JPY.** `get_pip_value(self.metadata.pairs[0])` resolves the pip value **once from a hard-coded first pair** and reuses it for every pair traded, so on USD_JPY every pip quantity is **100× too small**. **13 research strategies use the idiom in code**; it is load-bearing on the stop in four of them: `riding_trend_retracement` (stop÷ATR **0.021** of its non-JPY value, mean R **−4.585**), `smashing_forex_2` (**0.042**, **−1.065**), `three_candle_swing_reversal` (0.468, −1.153), `liquidity_sweep_ob` (0.813, −1.271). The other constant-pip strategies sit at 0.87–1.02, indistinguishable from the ten that resolve the pip correctly (0.82–1.12). **The engine is clean** — v1 lands on exactly 1.500 × ATR for USD_JPY as for every other pair. This is the whole of the USD_JPY H4 anomaly (cell mean −0.229; **+0.012** without the two worst), and `smashing_forex_2` alone moves the bank from −0.0701 to **−0.0575**. **The underlying cause is a contract gap:** `StrategyV2.generate_orders(frames)` is never given the pair it runs on, so a strategy needing a pip size must find it elsewhere. `double_bottom_measured_move.py:47-53` names the hazard verbatim, and three strategies wrote `_pip_size_from_price` to work around it. **Fix, two levels:** per file, resolve the pip per pair or adopt `_pip_size_from_price`; structurally, pass the pair to `generate_orders` so the gap cannot be re-entered. Needs a `forex-strategist` + `leakage-hunter` pass and a `persist_all` rebuild, so it re-derives outcomes → attribution → map → weights. Neither priority-1 strategy is in the live map today. | `audit/reports/engine_validation_2/report.md` §Q5.2, §B4; `q5_pip_idiom_by_strategy.csv`; `smashing_forex_2.py:62,88-90` | owner |
| O-29 | **The v2 stop-first exit rule costs a measured +0.026 R at H4 and +0.029 R at D1, and M15 bars can resolve half of it.** Against M15 ground truth (2.6M bars back to 2006), 6.5% of v2 H4 OOS exits are same-bar stop/TP collisions; resolving them correctly moves v2 H4 from −0.1057 to −0.0799. v1 is unaffected (+0.003). **48% of the collisions are unresolvable even at M15** (both levels inside one 15-minute bar), so the honest fix is M15 resolution plus an `exit_ambiguous` flag on the remainder. **Not applied:** changing the exit rule invalidates every row of `fact_trade_outcomes` and forces a full rebuild → attribution → map → weights re-baselining. The before/after already exists in `q4_counterfactual_R_v2_measured.csv`. **Owner decision.** | `audit/reports/engine_validation_2/report.md` §Q4, §B1 | owner |
| O-12 | **T6 ATR case-mismatch** — `engine_adapter` writes `df["atr"]`, `StrategyBase` reads `df["ATR"]`, so T6 stops are warmup-dependent. No FIX doc. Research verdicts only, not the live path. | carried from the previous register | — |
| O-13 | **`layer2_config_adapter` emits un-runnable T-SQL.** Long-standing, unchanged. | carried from the previous register | — |

## Scheduled

| # | Item | Evidence | Owner |
|---|---|---|---|
| O-19 | **`telemetry/signals/` is written hourly with no ENFORCED retention.** GOVERNANCE.md §1.4 requires a stated retention for anything on a cadence. The local side is stated and enforced (90 days, `publish_ledger --prune`); the remote side is stated only — `REMOTE_RETENTION_DAYS = 365`, `REMOTE_RETENTION_ENFORCED = False`. No GCS lifecycle rule exists anywhere in this repo and `system1-rw` is storage-scoped, so it may lack the bucket-admin right to set one. Sharpened by the local prune: from day 91 the unbounded remote is the sole archive of record. **Scope any lifecycle rule to `telemetry/signals/`, never `telemetry/` or `system1/`** — `delete_prefix` is an undelimited prefix match and the wider prefixes hold `s1_health.json` and the model bundles. | `src/signals/publish_ledger.py` REMOTE_RETENTION_*; structure-warden pass 2026-08-30 | owner |
| O-25 | **The signal ledger has no index object, so Systems 2/3 cannot discover it.** **Partial fix 2026-09-03:** `publish_index()` added to `src/signals/publish_ledger.py` — writes `telemetry/signals/index.json` via `atomic_pointer_update` with schema_version, generated_at, and per-day chunk listings (key, sha256, rows, size_bytes). Chunk metadata accumulated in `ledger_publish_state.json`. CLI: `--index --index-live`. Default `dry_run=True`. **Deployment blocked on Q4 (has System 2 built the LIST path?) and O-19 (retention decision).** `release-guard` reviewed design: index key is inside `telemetry/signals/` prefix — collateral-deletion risk from broad `delete_prefix` sweep documented at every call site. 5 tests pass. See `task/2026-September-week1/signal-emission-defects/DELIVERABLE.md`. | `src/signals/publish_ledger.py`; commit `acaa71d`, branch `fix/signal-emission-defects-d6-d7-d8` | owner |
| O-5 | **Retrain hold expires 2026-09-15.** It never gated the outcomes writer and did not cause the staleness. On expiry the retrain republishes a map — that republish must land on fresh evidence, so O-2 comes first. Either the underlying reason is resolved or the hold is renewed with a fresh one; silent renewal is an open issue in disguise. | `results/state/cron_holds.json` | owner |
| ~~O-6~~ | ~~Re-snapshot the crontab backup.~~ **DONE 2026-08-29** — `results/state/crontab.backup-20260829.txt`, taken before installing the outcomes job. The 2026-08-02 backup listed 3 jobs against 5 installed; that drift is what left the freshness model assuming a Saturday-only ingest. **Re-snapshot whenever the crontab changes.** | `results/state/crontab.backup-20260829.txt` | — |

## Announce

| # | Item | Evidence | Owner |
|---|---|---|---|
| O-7 | **Tell System 2/3 about the additive contract fields.** `data_through_utc`, `evidence_age_days`, `outcomes_written_at_utc` now appear on the regime map, weights, and strategy-stats documents. Additive and optional, so nothing breaks — but `contracts/README.md` requires the change be agreed and documented in `docs/comms/`. Message is drafted and unsent. | `docs/comms/to_system2/TO-SYSTEM2-3-2026-08-29-evidence-age-fields.md` | owner |
| O-8 | **Note to Computer 2 on the M2 answer.** Deferred by owner decision; they were holding their pipeline pending this exact decision and the answer now exists. Still unsent. | carried from the previous register | owner |
| ~~O-18~~ | ~~Reply to the Signal-Telemetry RFC.~~ **SENT 2026-08-30** — `docs/comms/to_system2/TO-SYSTEM2-3-2026-08-30-signal-ledger-and-rfc-corrections.md`, seven corrections. RFC filed as received at `docs/comms/replies/S2-3-RFC-2026-08-30-decoupled-signal-telemetry.md`. A `comms-liaison` pass before send caught two false claims (designated count 11→12; a `scored-signals.heartbeat` 404 fixed on 2026-08-23) that would have frozen permanently. **Follow-up owed:** publish `dlq_count` into `s1_health.json` — §5 tells them wire-drop detection is impossible without it. | the sent message | owner |

## Deferred by decision

| # | Item | Evidence | Owner |
|---|---|---|---|
| O-9 | **R3 regime-aware trial — experiment complete, decision open.** Framework works (equivalence test passes); the result does not support regime conditioning as an edge — the winning arm was pair selection, and every profitable cell had a PF confidence interval straddling 1.0. The durable output is `docs/design/STRATEGY_EXPERIMENT_STANDARD.md`. `src/regime_aware/` has since been removed, which is what orphaned the 9 `*_RA` registry rows in O-3. Decide: adopt the standard / port more strategies / consider it closed. | `task/2026-August-week2/deliverables/T3-regime-aware/README.md` | owner |
| O-10 | **v2 promotion gap is undocumented.** Decided NOT to build the promotion path (correctly — don't build the door before anyone comes through it), but the gap itself was never written down, so it stays invisible until someone hits it. | carried from the previous register | owner |
| O-11 | **Repo cleanup** — `task/BACKLOG-repo-structure-and-cleanup.md`. Parked deliberately. | carried from the previous register | owner |
| O-14 | **Finding A — weight starvation** at 8e-8. Genuinely premature: cannot matter until M2. | carried from the previous register | — |
