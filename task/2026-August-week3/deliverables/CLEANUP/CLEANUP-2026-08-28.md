# CLEANUP — 2026-08-28

Third structural pass. Trigger: the repo root had accumulated 9 new violations of the
"nothing new at root" rule since the 2026-08-16 pass, `docs/comms/` had an in-flight
reorganisation that needed committing, and several map drift items in `STRUCTURE.md` /
`CLAUDE.md` had gone stale (most visibly: "0 signals emitted" while the live state showed 46).

Executed in three commits on branch `cleanup`.

---

## Workstream 1 — Root cleanup

### Moved from root

| From | To | Reason |
|---|---|---|
| `generate_ranking_report.py` | `shell/generate_ranking_report.py` | One-off analysis script; no imports |
| `generate_reference_vector.py` | `shell/generate_reference_vector.py` | One-off analysis script; no imports |
| `generate_report.py` | `shell/generate_report.py` | One-off analysis script; no imports |
| `SYSTEM_ARCHITECTURE_EXPLANATION.md` | `docs/design/` | Prose doc belongs in docs |
| `docs/BUNDLE-CONSUMER-GUIDE.md` | `docs/design/BUNDLE-CONSUMER-GUIDE.md` | Was at wrong docs level |
| `SEND-TO-TRADING-1.md` | `docs/comms/to_system2/` | Comms file — moved to correct subdir |
| `goal/2026-08-AUGUST-WEEKEND-GET-TRADING-STARTED.md` | `docs/goals/` | `docs/goals/` is the mapped destination; `goal/` dir removed |

### Deleted from root — evidence of no references

| File | Evidence of no reference | Reason |
|---|---|---|
| `run_R0.py` | `grep -rn "run_R0"` found only the prompt doc and the 2026-08-16 cleanup record | Imports `src.regime_aware.context` (no longer exists); R3 experiment concluded |
| `test_p1.py` | `grep -rn "test_p1"` → 0 results | One-off verification script for 2026-08-15 outcomes rebuild; task complete |
| `test_eval.py` | `grep -rn "test_eval"` → 0 results | Scratch gatekeeper eval; no task association |
| `scratch/extract_docs.py` | `grep -rn "extract_docs"` → 0 results | Imports `src.regime_aware.strategies` (dead); R3 concluded |
| `model001_ingest.log` (root) | N/A — log file | Stale root copy (2026-08-11); `logs/` holds current version |
| `model003_regime.log` (root) | N/A — log file | Stale root copy; `logs/` holds current version |
| `docs/architecture/adr/FIX-S1-015-mba-ingest.md` | `grep -rn "architecture/adr"` → 0 results | 1-line completion stub, no references, superseded by proper FIX docs |

### Credential backups

`.env.bak-20260817` and `.env.bak-20260822T172002Z` were moved from the repo root to
`secrets/` (git-ignored). Both were already git-ignored at the root. They were NOT deleted
because the 2026-08-17 backup is smaller (1706 bytes vs 2064 bytes current) and may contain
a credential not in the current `.env`. The owner should verify and delete when confident.

### docs/comms/ reorganisation committed

The in-flight move of `docs/comms/*.md` → `docs/comms/to_system2/` was committed as-is.
All 13 files that were deleted from the flat level are present in `to_system2/`.
15 additional files that were only in `to_system2/` are now also committed.
No sent message was lost.

---

## Workstream 2 — Misplaced files

| From | To | Reason |
|---|---|---|
| `src/layer0/README_LAYER0_INTEGRATION.md` | `docs/design/` | Prose doc in `src/`; STRUCTURE.md "Do NOT put here" |
| `src/layer0/README_SWING_ENGINE.md` | `docs/design/` | Same |
| `src/regime/websitereviewonhmm.md` | `docs/research/` | Third-party article paste; belongs in research |
| `docs/TOP-10-CANDIDATES-2026-08-23.md` | `docs/research/` | Research note at wrong docs level |
| `results/state/DONE_*_agent_*.md` (9 files, 2026-06-24) | `docs/worklog/` | Hand-authored completion records in machine-written folder |
| `results/hmm_all_eval.log` | `logs/` | Log in results; `logs/` is the mapped destination |
| `results/hmm_d1_eval.log` | `logs/` | Same |

---

## Workstream 3 — Routine deletion

### `results/reports/` — retention policy applied

Policy (recorded in `results/README.md` for future passes):

| Prefix | Keep | Deleted |
|---|---|---|
| `ingest_manifest_*` | 7 most recent | 138 older files |
| `dq_gap_report_*` | 7 most recent | 138 older files |
| `vetting_report_*` | All 33 | — |
| `attribution_report_*` | All 29 | — |
| `qualification_report_*` | All 24 (in reports/) | — |
| `STRATEGY_RANKING.md` | Kept | — |

Pre-delete check: `attribution_report_20260815T093601Z.json` is cited by
`docs/comms/notices/S1-NOTICE-2026-08-15.md` and `task/2026-August-week2/deliverables/T3/DELIVERABLE.md`
— kept. No ingest_manifest or dq_gap_report files were cited by any doc.

### `results/` root-level duplicate qualification reports

40 `qualification_report_*` files (JSON+MD) at `results/` root were deleted. All 24 unique
reports already existed in `results/reports/`. The duplicates were a residue of the pre-`reports/`
layout and were confirmed identical by diff. `results/qualification_progress.json` also deleted
(superseded by progress tracking in the pipeline itself).

### Cache cleanup

Root `__pycache__/` deleted (git-ignored, regenerates). No `.DS_Store`, `*.orig`, `*.rej`, or
`*.tmp` files found anywhere in the tree.

---

## Workstream 4 — READMEs added

9 new README files:

| Path | What it covers |
|---|---|
| `contracts/README.md` | Schema usage, cross-system change rules |
| `results/README.md` | Machine-written output only; retention policy; protected state files |
| `shell/README.md` | Cron script table (do-not-rename warning), analysis scripts |
| `issues/README.md` | Issue inbox format; `issues/` directory created |
| `docs/comms/README.md` | Subfolder structure; append-only rule |
| `docs/goals/README.md` | Goals vs tasks guidance; file inventory |
| `docs/design/README.md` | All design docs listed; design vs task guidance |
| `docs/research/README.md` | Research scope; file inventory |
| `docs/worklog/README.md` | Worklog vs task distinction |

---

## Known map drift fixed

| File | Fix |
|---|---|
| `STRUCTURE.md` | Removed stale `src/regime_aware/` row (folder removed with FIX-S1-016) |
| `STRUCTURE.md` | Updated Last-updated line |
| `CLAUDE.md` | "Signals emitted: 0" → **46** (confirmed from `signal_emitter_state.json`) |
| `CLAUDE.md` | Hard-coded 6-cell map table replaced with pointer to `regime_strategy_map.json` |
| `CLAUDE.md` | Crontab re-verified 2026-08-28 (unchanged from 2026-08-23) |
| `CLAUDE.md` | Updated Last-updated line |

---

## Deliberately not touched

1. **`.env.bak` files** — moved to `secrets/` but not deleted. Owner must decide.
2. **`docs/strategy-notes.json`** at the `docs/` root — unclear provenance (neither hand-authored
   prose nor machine output). Filed in issues for investigation.
3. **`src/layer0/promotion/layer2_strategy_map.txt`** — could not confirm whether the promotion
   path reads it directly. Left in place. Filed in issues.
4. **`results/state/` accumulated retrain logs** (~300 files) — machine-written, correct location.
   Not part of scope.
5. **`archieved/` unpacked trees** — cited by `CLAUDE.md` by path. The 2026-08-16 cleanup noted
   this and deferred it. Still deferred; owner decision required.
6. **`test_eval.py` (untracked)** — was deleted from root; appeared as untracked in `git status`
   because it was never committed. Confirmed deleted from disk.

---

## Verification

```
Before: 4 failed, 596 passed (src/monitoring/tests/test_model_card.py — pre-existing)
After:  4 failed, 596 passed — no change
```

Cron scripts: `bash -n shell/cron_hourly_signals.sh` passes.
Imports: `src.signals.run`, `src.signals.build`, `src.ingestion.multi_timeframe_ingest`,
`src.monitoring.heartbeat` all import cleanly.
