
1. The learning loop is severed. src/layer0/strategies/__init__.py is still missing (I just confirmed), so the only trade-outcomes writer has been broken since June. Every retrain silently re-derives verdicts from stale data. A self-improving trading system that can't see its own trade results is not doing what it's meant to do — this is the single most corrosive open issue.
2. The truth you already built isn't live. Seven System-1 fixes (S1-001, 002, 004, 005, 006, 009, 010) are implemented and verified but sitting "log-only, pending sign-off." The live champion and strategy map still reflect the pre-fix, partially-leaky world. The work is done; the value is zero until promoted.
3. The money-touching layer is the least fixed. Every System-3 fix is still "Proposed": correlation gates blind (S3-001), exposure cap unit confusion (S3-002), Kelly sizing inert on stale edge (S3-003), risk caps in quote-currency units (S3-004), auditor leakage (S3-005), plus the live sizing-gate lockout. And that sizing code exists only on the VM — not in any repo.

This week's tasks

Workstream 1 — Reconnect the feedback loop (small fixes, biggest payoff)
- Add the missing __init__.py and fix the un-importable strategy directories (Mean Reversion , Volatility Expansion and Compression — trailing spaces and spaces in names make these broken Python packages regardless).
- Backfill trade outcomes from June to now, then re-run the OOS re-measurement that FIX-S1-008 unblocked (fact tables have 2000+ rows — this is real data waiting to be used).
- Make import failures loud: any pipeline that falls back to stale data on an import error should crash instead.

Workstream 2 — Promote the verified work (a decision, not a build)                                                                                                         - Sign off and promote the corrected strategy map + leak-free champion tack). Fix the beats_incumbent 0.965 ratchet while you're in there, anddecide the GATEKEEPER_AUTOPROMOTE staged-rollout next step.                                                                                                                
Workstream 3 — De-risk the money layer (big, this is the week's heavy lift)                                                                                                - Pull the Layer-5 sizing code off the VM into version control — before.
- Work the S3 fixes in risk order: unit-confusion bugs first (S3-002, S3-004 — wrong-units position sizing is how accounts blow up), then the sizing-gate lockout, then    correlation gates and Kelly.
                                                                                                                                                                           Workstream 4 — Kill the silent-failure pattern (small, high leverage)
- One daily heartbeat script that asserts freshness of prices, trade outcomes, and telemetry, and alerts when stale. Ingest was dead 16 days and outcomes ~2 months before anyone noticed — this is the cheapest insurance you can buy.
- Rotate and remove the committed DB password (FIX-XC-003). Add a .env.example. Small, do it Monday.                                                                    
Workstream 5 — Innovation: the research strategy engine                                                                                                                 Yes, build it — but build it as the fix for the strategy-packaging messdy have strategies/research/ and strategieStaged/ as informal stages.Formalize it: a strategy contract (one interface every strategy implements), a registry, and a promotion pipeline research → staged → qualified that runs the same leak-gates the gatekeeper now has. That gives you the sandbox and makes Works can start as a design doc + contract definition this week even if the full engine takes longer.                                                                                                                                               
Industry practices you don't follow yet                                                                                                                                 
- Live code outside version control (Layer-5 sizing on the VM) — nothing running in production should lack a source of truth in git.                                    - No CI — you have real test suites (143 tests green in S1-009's reportothing runs them automatically on push.
- Secrets in the repo (FIX-XC-003).                                                                                                                                     - No freshness monitoring / dead-man switches — every outage so far was
- Fail-silent instead of fail-fast — broken imports degrade to stale data instead of crashing.                                                                          - No single source of truth for config — the Layer-2 DB drifted from Lad manual re-sync; a nightly reconciliation check would catch this.Related: the adapter still emits un-runnable T-SQL.                                                                                                                     - Stale pointer files kept alive (telemetry/latest.json dead but still e real one) — dead artifacts should be deleted, not documented around.
- No backtest-vs-live reconciliation — nothing compares live fills/slippage against backtest assumptions, which is the standard way desks detect that their edge estimatare fantasy.
- Promotion by ad-hoc sign-off — seven verified fixes are queued behind an informal approval step; a lightweight release checklist would stop good work from rotting in limbo.

If I had to compress the week to one sentence: reconnect the outcomes lerified, and get the money-touching code under version control andunit-correct — the research sandbox is the reward for doing those first.
