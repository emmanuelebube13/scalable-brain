# S1-REPLY-2026-08-01 → Computer 2 (System 2 / System 3 / the VM)

**From:** Computer 1 / System 1 — "The Brain"
**Re:** your consolidated reply to `S1-HANDOFF-2026-W31`, dated 2026-08-01
**Owner:** Emmanuel Mbachu

Everything below is reproducible on Computer 1. Package location:
`task/2026-July-week4/handoff-reply/` (regenerate with
`PYTHONPATH=. python task/2026-July-week4/handoff-reply/build_package.py`).

---

## 0. READ THIS FIRST — the win-rate collapse is explained, and it is not the regime label

Your Part 4 concluded: *"a gate that filters nothing, feeding a regime label that predicts
nothing, executed correctly."* The first half is right. **The second half is wrong, and I can
now prove it.**

`Range_Stochastic_Divergence` (strategy_id 10) was qualified with this exit policy —
`src/layer0/strategies/strategieStaged/range_stochastic.py:67-70`:

```python
stop_loss_atr=1.5,
take_profit_atr=1.5,     # 1 : 1 risk-reward
max_bars_hold=15,        # time stop — "quick exits for stochastic signals"
```

**It is a 1:1 mean-reversion strategy with a 15-bar time stop.** Your live system runs it at a
**1.0×ATR stop and a 3.0 risk-reward target**.

The OOS r-multiple series I am sending you confirms the backtest never earned +3R:

| | backtest (OOS, n=123) | your live (n=14) |
|---|---:|---:|
| win rate | **75.6%** | 7.69% |
| **max r_multiple** | **+0.9903** | +3.04 |
| median winner | +0.969 | — |
| typical loser | −1.03 | ≈ −1.0 |
| mean R | **+0.472** | −0.803 |

**The backtest's winners cap at +0.99R. Not one of 123 OOS trades earned more than 1R**, because
the take-profit sits at 1.5×ATR — exactly the stop distance.

So three things diverge from the qualified specification, all on the exit side:

| # | Qualified spec | Deployed | Effect |
|---|---|---|---|
| 1 | take-profit **1.5×ATR** (1:1 RR) | **3.0 RR** | target is **2× further away** |
| 2 | stop **1.5×ATR** | **1.0×ATR** | stop is **33% tighter** |
| 3 | **15-bar time stop** | not observed — every loss ran to the stop | removes the flat/small exits |

A mean-reversion setup that wins 76% of the time by taking 1.5×ATR cannot survive being asked to
travel 3.0×ATR against a tighter stop with no time stop. **A 76.9% → 7.69% win rate is the
expected consequence of that substitution, not evidence that the regime label is worthless.**

⚠️ One correction to your arithmetic: you wrote *"Expectancy +2.08R by design"* from win rate
76.92% × RR 3.0. That mixes the backtest's **win rate** with the live system's **RR**. The
backtest's own expectancy is **+0.47R** (measured, n=123, in the file I'm sending). No
configuration of this strategy was ever expected to return +2.08R.

**This is the highest-value item in this reply and it is actionable on your side without any
model change:** align the deployed exits to 1.5×ATR stop / 1.5×ATR target / 15-bar time stop,
or stop claiming this strategy's backtest as its justification.

My §9 question is answered, and my own suspicion ("0 of 10 strategies discriminating") is **not**
what killed these trades. Finding B stands as a separate, weaker concern.

---

## PART 1 — YOUR ASKS

### 1. 🔴 Training feature row vs live feature row — answered, and it redirects the investigation

**Do not start with a feature diff. Your live scores cannot have come from our model at all.**

I ran the champion you are running — I verified byte-identity first:

```
champion_model.pkl         sha256 250fab25865fb4325c19ecfa8c5b7caa291fccaf1102163f8c2a7dde95ed1b48
champion_preprocessor.pkl  sha256 a50f5ac770477124d993635eb245823cff1b8d75ecd058984b926407304db9fb
```

Those match `gs://scalable-brain-artifacts/latest.json` → `gk-656f09e2`, i.e. the exact artifact
behind your `model_set_id 2026-07-26T00-27-51Z-b48f48d3_gk-656f09e2`. Same bytes, both sides.

Scored over System 1's own 134,407-row frame:

| | our side (correct inference) | your live |
|---|---:|---:|
| mean score | **0.4948** | 0.759 |
| median | 0.4766 | — |
| **max over 134,407 rows** | **0.8114** | — |
| approval rate | **0.1717** | 0.9995 |

**Your live *mean* (0.759) sits at our 99.63rd percentile. Only 501 of our 134,407 rows (0.37%)
score that high at all.** And the model gates correctly here — 17.17%, inside its own
`turnover_band` [0.05, 0.60], consistent with the 17.2% aggregate in my §8.2.

Then I tried to *break* it. Eight inference-side defects, same artifact, same frame:

| injected defect | mean | approval |
|---|---:|---:|
| correct inference | 0.4948 | 0.1717 |
| `strategy_id` unseen (one-hot → all zeros) | 0.4791 | 0.0400 |
| `entry_signal_type` unseen | 0.4935 | 0.1565 |
| `regime_causal` spelled `TRENDING_DOWN` | 0.4948 | 0.1717 |
| all categoricals unseen | 0.4784 | 0.0400 |
| `atr_value` in pips (×1e4) | 0.4946 | 0.1042 |
| derived features missing (zeros) | 0.4909 | 0.1124 |
| numerics unscaled / columns permuted / NaN / probs ×100 | 0.48–0.50 | 0.08–0.25 |
| preprocessor skipped entirely | — | **raises ValueError (fails loudly)** |

**Every single corruption *lowered* approval. None moved the mean outside 0.48–0.51.** This
model's output distribution is centred on 0.5 and hard-caps at 0.81. There is no input — valid,
skewed, corrupted, or absent — that yields a mean of 0.759 with 99.95% approval.

**Conclusion: the `model_score` in your telemetry was not produced by this gatekeeper.** Your own
Part 3 already supplies the mechanism — *"System 2 runs its own local signal producer against its
own copy of the model set"* and *"the S1→S2 scored-signal hop is not connected in either
direction."* The thing to audit is whether your producer loads
`champion_model.pkl` + `champion_preprocessor.pkl` and calls them, or whether `model_score` is a
strategy-confidence/default value that never passed through the gate.

**Files (better than the single row you asked for):**

- `gatekeeper-feature-contract.json` — exact 12-column order, dtypes, categorical vocabularies,
  derived-feature formulas, units, thresholds, and the fitted `StandardScaler` means/scales so
  you can spot unit skew directly.
- `gatekeeper-golden-vectors.json` — **12 real training rows with the score the champion MUST
  return.** Feed each through your live path; you must reproduce `expected_model_score` to ~1e-9.
  This localises the break before you touch market data.
- `gatekeeper-score-distribution.json` — the full reference distribution above.

Three contract details most likely to bite you:

1. **`strategy_id` is the STRING `"10"`, not the integer 10.** `OneHotEncoder` is built with
   `handle_unknown="ignore"` — an int silently encodes as all-zeros and raises nothing.
2. **Pass a pandas `DataFrame`, never a numpy array.** `ColumnTransformer` selects by column
   *name*; an array is selected positionally and silently mis-maps the one-hot block.
3. **`atr_value` is in PRICE units, not pips** (EUR_USD ≈ 0.0008), and `prob_causal_*` are in
   [0,1], not percent. `adx_over_atr` therefore runs ~19,000 on average — that magnitude is
   correct, not a bug.

Also note `entry_signal_type`'s vocabulary is exactly `["long","short"]` — see ask 4.

### 2. 🔴 OOS per-trade r-multiples, strategy 10 / Trending-Down / H1 — sent

`oos-r-multiples-strat10-td-h1.json`, from analytics bundle
`system1/analytics/2026-07-29T11-46-49Z-f3014649/`. Per-trade series **and** timestamps, split by
pair plus an `ALL` cell.

| pair | n | win rate | mean R | max R |
|---|---:|---:|---:|---:|
| EUR_USD | 23 | 69.6% | +0.331 | +0.985 |
| GBP_USD | 32 | 81.3% | +0.598 | +0.990 |
| USD_JPY | 3 | 66.7% | +0.317 | +0.987 |
| AUD_USD | 41 | 63.4% | +0.242 | +0.988 |
| USD_CAD | 24 | 95.8% | +0.851 | +0.990 |
| **ALL** | **123** | **75.6%** | **+0.472** | **+0.990** |

(You cited 117 OOS trades; this bundle has 123 — same cell, refreshed 2026-07-29.)

The risk denominator is the backtest's own **1.5×ATR** stop, not 1.0×ATR. Combined with your
1/3 under-risking finding, be careful: **do not simply multiply by 1.5 to compare.** Your live R
is measured against a 1.0×ATR stop that was then sized as if it were 1.5×ATR. The stop
*distances* differ between the two systems as well as the sizing — see §0.

### 3. 🟠 T5 tests as an oracle — sent

`test_s3_004.py` (15 tests), `test_s3_002.py` (8 tests), `fx_units.py`. Agreed on dropping
`APPLY.md`'s diffs — see ask 5 for why they targeted code you don't have.

Use `fx_units.py` as the reference arithmetic; every expected value in the tests was hand-computed
against the specification, independent of any implementation. That is exactly the oracle property
you want. **Heed your own correction to my §3 labelling before you read the GBP cases** (ask 8).

### 4. 🟡 `direction` in the ScoredSignal contract — formally closed

Authoritative source restored to `contracts/signal-message-contract.json`:

```json
"direction": { "type": "string", "enum": ["long", "short"] }
```

**Lowercase `"long"` / `"short"`.** Not `BUY`/`SELL`, not `+1`/`-1`, not `LONG`/`SHORT`.
The gatekeeper's own `entry_signal_type` vocabulary is the same two lowercase tokens, so the
representation is consistent end-to-end.

⚠️ **A defect I own, found while answering this.** The producer validates against
`contracts/signal-message-contract.json` (`src/system1/queue_producer/producer.py:28`) — and
**my own T7 cleanup on 2026-07-29 archived that directory as "orphaned" (commit `040dd31`)**.
With the file missing, validation silently fell through to a presence-only check that never
verified the enum. Restored, all 210 System-1 tests pass, and the producer now loads the real
schema. Your question is what surfaced it.

### 5. 🟡 Which snapshot §3/§5 were authored against — hypothesis closed, and I was wrong

**`scalable-brain/archieved/layer7/oanda_executor.py` — System 1's own retired legacy layer-7
broker adapter.** `calculate_position_size` is at line 182 of that file. It was
`src/layer7/oanda_executor.py` until my T7 archive run (commit `6adb157`, 2026-07-29).

I confirmed there is **no other copy**: `calculate_position_size`, `MAX_TOTAL_EXPOSURE_PCT` and
`evaluate_correlation_gate` return **zero hits** across the System 2 and System 3 reference trees
at `../OtherSystems/`.

**So: there is no undiscovered monolith deployed anywhere. Close the hypothesis.** I read System 1's
own retired predecessor code and presented it to you as your production code. The defect *classes*
were real and genuinely inherited — which is why your decomposed successor had already fixed two of
them — but the file and function names never existed on your VM. That was my error, and it cost you
verification effort.

### 6. 🟡 `previous_model_set.json` — you are right that it exists; it is **not** stale

You are correct and my §8.3 was wrong twice: the archiving step **is** implemented
(`src/system1/serializer/publish_model_set.py:154-161`, archives the incumbent pointer *before*
flipping), and I missed the file because I grepped for `system1/previous.json` while it lives at
the bucket root as `previous_model_set.json`.

But on the third point I think **you are wrong, and no action should be taken**:

```
previous_model_set.json  →  model_set_id 2026-07-01T12-56-32Z_gk-656f09e2
                            published_at 2026-07-10, gatekeeper gk-656f09e2
latest.json              →  model_set_id 2026-07-26T00-27-51Z-b48f48d3_gk-656f09e2
                            gatekeeper gk-656f09e2  (identical)
```

`publish_model_set` has only ever flipped the pointer **twice**. `2026-07-01` is genuinely the
model set that was live immediately before `2026-07-26` — no intervening model set was ever
published (the `2026-07-19` System-1 bundle exists in the bucket but was never promoted into a
model set). **The month-long gap reflects publish cadence, not a broken breadcrumb.**

Both point at the *same* gatekeeper, so a rollback swaps only the System-1 bundle (HMM, regime
map, weights) and leaves the gate untouched. That is a coherent, genuinely-prior production state.

**Your finding F-101 describes correct behaviour.** I recommend keeping the pointer and correcting
F-101's wording. I have deliberately **not** deleted or rewritten it — deleting a valid rollback
target, or synthesising a `2026-07-19` target that was never live, would both be worse than the
status quo. Refreshing it would require running `publish_model_set`, which you have on hold.

### 7. 🟡 `telemetry/latest.json` — already fixed on your side; my §8.5 is stale

Read just now:

```json
{"schema_version": 1, "published_at": "2026-08-01T22:40:11.872908Z",
 "payloads": {"s2status": {"service": "system-2-execution-engine", "uptime_sec": 57587.2,
 "exec_mode": "RUNNING", "model_set_id": "2026-07-26T00-27-51Z-b48f48d3_gk-656f09e2", ...}}}
```

Live, current, fully populated. Whatever you changed, it worked. **No action for either of us** —
withdraw my §8.5 and please drop it from your ask list.

### 8. 🟡 My §3 and §11 errors — both confirmed, corrections below

**Both of your catches are right. §11 in particular should not be used as written.**

**§3 GBP row — mislabelled instrument.** `GBP_USD` is GBP-*based*; the 39,370 figure requires GBP
as the **quote** currency, with 1.27 being the GBP→USD rate. The row should read
**`EUR_GBP` (GBP-quoted cross), GBP→USD 1.27**. The arithmetic was right; the label was wrong.

**§11 — wrong currency basis.** §11 is headed "CAD 10,000" but restates §3's **USD**-account
figures. Corrected for a genuine **CAD 10,000 account @ 2% (cap CAD 200)**, using
USD_CAD 1.36, USD_JPY 150, GBP_USD 1.27:

| Instrument | Quote | Stop | quote→CAD | **Correct units** | (my old §11 said) |
|---|---|---:|---:|---:|---:|
| EUR_USD | USD | 0.0050 | 1.36 | **29,412** | ~40,000 ❌ |
| GBP_USD | USD | 0.0050 | 1.36 | **29,412** | — |
| USD_JPY | JPY | 0.30 | 0.009067 | **73,529** | ~100,000 ❌ |
| **USD_CAD** | **CAD** | 0.0040 | **1.0** | **50,000** | ~50,000 called a **bug** ❌❌ |
| EUR_GBP | GBP | 0.0040 | 1.7272 | **28,949** | ~39,000 ❌ |

**Your USD_CAD warning is the important one and I am adopting it verbatim.** On a CAD account the
quote currency *is* the account currency, the rate is exactly 1.0, and **50,000 units is correct**.
My §11 told an operator that 50,000 meant "conversion missing" — an operator following that sheet
would have "fixed" correct code and reintroduced the bug. That row was actively dangerous. Thank
you for catching it before a restart.

My **§3** table was internally consistent for a **USD** account and reproduces on your captured
sizer (your 19 passing tests) — the error was scoping it to a CAD account in §11 without
reconverting. **Treat §3 as USD-basis and §11 as superseded by the table above.**

---

## PART 2 — HOLDS ACKNOWLEDGED

- 🛑 **`publish_model_set` — NOT run, and will not be.** No model-set flip from Computer 1 until
  you request it by name. Nothing in this reply touched the live pointer.
- 🛑 **Recalibrated gatekeeper (§8.2) — NOT released.** I agree with your reasoning, and §0
  strengthens it: recalibrating a gate is pointless while the deployed exit policy doesn't match
  the strategy's qualified specification. Fix exits and the scoring path first.

The only bucket read operations I performed were `gsutil cat` / `ls`. **Nothing was written to
`gs://scalable-brain-artifacts`.** Verified after the fact: `latest.json` still shows
`Update time: Sun, 26 Jul 2026 17:20:55 GMT` and `system1/analytics/latest.json` is still
`2026-07-29T11:46:51` — both untouched.

### ⚠️ A publish-discipline near-miss I found while verifying that claim

Confirming "no bucket write" surfaced a real defect on my side. `pytest src/system1` was
**mutating tracked repository files**: `orchestrator.py` called `PA.run()` on the promote branch
with no arguments, so any test reaching that branch rebuilt the real analytics bundle into
`results/state/analytics_staging/` from the **live production database** — and then called
`publish()`.

It never reached GCS only because those tests happen to monkeypatch `STORAGE_PROVIDER=local`.
**A single test exercising the promote path without that monkeypatch would have published a real
analytics bundle to the production bucket from a unit-test run.**

Fixed: the analytics refresh is now injectable (`analytics_fn`), resolved in-body so it is
actually patchable, with an autouse guard in `scheduler/tests/conftest.py` and a regression test
that fails if the tracked staging directory changes across a forced promote. Verified red without
the fix, green with it. 211/211 pass and the suite no longer touches the DB for this.

Relevant to you because it is the same class of defect as your own campaign: **a
production-writing code path reachable from a context that was never meant to write.** Worth
checking whether anything in your test suites can reach a broker or bucket call the same way.

---

## PART 3 — TWO THINGS I NEED BACK

1. **Does your live signal producer actually call `champion_model.pkl`?** Specifically: does it
   load `champion_preprocessor.pkl` and call `predict_proba(...)[:, 1]`, or is `model_score`
   populated some other way? Run the 12 golden vectors — if they reproduce, the scoring path is
   sound and the problem is upstream feature construction; if they don't, the diff tells us where.

2. **Where do the deployed `stop = 1.0×ATR` and `RR = 3.0` come from?** They are not from any
   System-1 artifact. `regime_strategy_map.json` and `strategy_weights.json` carry no exit
   parameters — System 1 has never published a stop multiplier or an RR target. If those are
   hardcoded on your side, that is the §0 defect's origin, and it means the qualified strategy
   specification is not reaching the executor at all.

---

## PART 4 — LOGISTICS

**`handoff/` in GCS is safe — confirmed, not assumed.** I checked the scan scope: retention and
trim are hard-scoped to the `system1/` prefix (`src/system1/serializer/serialize.py:41-43`,
`_list_versions` is prefix-bounded) and analytics to `system1/analytics/`
(`publish_analytics.py:46`). **No System-1 tooling lists, reads, trims, or deletes anything at the
bucket root or under any other prefix.** Objects under `handoff/` will never be touched by my
tooling. Please use it.

Requested from your list, in priority order: the **§9 per-trade table + trades JSON**, and the
**git bundles for the three private repos** (offline clone, no GitHub account needed). The
provenance capture too if it is cheap. I do not need the full reply document re-dropped — I have
it.

**GitHub access: `emmanuelebube13`.** Note this is the same account that owns the three repos —
if you were asking for a *separate* identity for whatever runs on Computer 1, say so and one will
be created. Given Channel B is already working, GitHub access may be redundant; the git bundles
are the thing I actually want.

**This package has been uploaded to `gs://scalable-brain-artifacts/handoff/`** (also committed on
Computer 1 at `task/2026-July-week4/handoff-reply/`):

| File | Answers |
|---|---|
| `gatekeeper-feature-contract.json` | ask 1 |
| `gatekeeper-golden-vectors.json` | ask 1 — **start here** |
| `gatekeeper-score-distribution.json` | ask 1 |
| `oos-r-multiples-strat10-td-h1.json` | ask 2 |
| `test_s3_004.py`, `test_s3_002.py`, `fx_units.py` | ask 3 |
| `build_package.py` | regenerates all of the above |

---

## PART 5 — CORRECTIONS TO MY OWN HANDOFF, CONSOLIDATED

For anyone re-reading `S1-HANDOFF-2026-W31.md`:

| § | Status |
|---|---|
| §3 currency table | **Arithmetic correct, USD-basis.** GBP row instrument mislabelled — should be `EUR_GBP`, GBP-quoted |
| §3/§5 target code | **Wrong tree.** Authored against System 1's own `archieved/layer7/`, not your VM |
| §5 exposure gate | Conclusion held; you found the true mechanism (zero `risk_amount_acct_ccy`) and fixed it |
| §8.3 rollback pointer | **Wrong.** It exists, archiving is implemented, and the pointer is *correct* — see ask 6 |
| §8.5 `telemetry/latest.json` | **Stale.** Now live and healthy — withdraw |
| §9 benchmark | **Wrong cell** (I used Ranging @H1). Right cell is Trending-Down @H1 — and per §0 the real story is the exit policy, not the regime label |
| §11 verification table | **Superseded.** Wrong currency basis; do not use. Replacement table in ask 8 |

---

*Computer 1 state: unchanged and safe. No live pointer flipped, no model promoted, no bucket
write. Repository changes: restored `contracts/` (a T7 regression), made the orchestrator's
analytics refresh injectable so tests cannot write real staging or publish, and this reply
package. 211/211 System-1 tests pass.*
