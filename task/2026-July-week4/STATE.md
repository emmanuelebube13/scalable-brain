# Week 2026-W31 — Execution State Ledger

**Protocol (every agent must follow):**
1. Read this file FIRST, before doing any work.
2. Skip any step marked `DONE`. Resume from the first step marked `IN_PROGRESS` or `PENDING`.
3. After completing each numbered step in a task, append a line to the log below **immediately** (not at the end of the session) — this is what makes credit-limit interruptions survivable.
4. Entry format: `| <UTC timestamp> | <task> | <step> | DONE/FAILED/BLOCKED | <one-line note: what was verified, or why it failed, or what unblocks it> |`
5. On FAILED: also append the root cause to the `## Failure log` section of that task's file, and add the corrected instruction into the task's execution plan.
6. On BLOCKED (needs the user / other computer / VM access): state exactly what the user must do, then move to the next unblocked task.

## Task status board

| Task | Status | Last step completed | Notes |
|------|--------|--------------------|-------|
| T1 reconnect-feedback-loop | **DONE** | 6 + deliverables | Outcomes current through 2026-07-24. 42 new + 173 existing tests green. Deliverables complete. Real root cause differed from the task's premise — task file corrected in place. |
| T2 secrets-and-env | **DONE** | 6 + deliverables | `sa` password rotated, old value verified dead, 27 occurrences purged from 11 tracked files, `.env.example` added, FIX-XC-003 IMPLEMENTED. History not rewritten (accepted risk — pending user decision if they want otherwise). |
| T3 promote-verified-work | **DONE** | 1-6 + deliverables | Owner signed off; bundle `2026-07-29T11-46-42Z-55dacdbf` PROMOTED — **first promotion ever where `beats_incumbent` actually compared** (`prefixed`, 0.965 ≥ 0.931225). Two gate defects fixed (FIX-S1-011 ratchet, FIX-S1-012 inert gate). 3 gaps left open: no `previous.json` rollback pointer (never implemented), model-set pointer still on the OLD bundle, uplift regressed ~6% unnoticed. |
| T4 heartbeat-monitoring | **DONE** | 6 + deliverables | 8 checks live, daily cron at 06:00 UTC installed. First run 8/8 PASS. Forced-failure demo confirmed exit 2 + alert flag + log. 27 new tests (242 repo-wide). |
| T5 derisk-money-layer | **DONE (capture BLOCKED)** | 3-6 + deliverables | Fix package complete: 23 tests, red-before evidence, APPLY/HANDOFF/TRIAGE/S3-006 root cause. **VM capture BLOCKED** — user must run the tar command in `deliverables/T5/DELIVERABLE.md` §1. **Critical finding: 10 live trades, ALL losers, PF 0.0, lifetime −15,934.81 CAD — the jammed gate is the only thing preventing further loss.** |
| T6 research-strategy-engine | **DONE** | 1-6 + deliverables | Contract + registry + promotion pipeline + 14 adversarial tests (all attacks blocked by code). Pilot `rsi_mean_reversion` REFUSED at the gate with 5 per-gate reasons over 9,806 OOS trades — a successful pilot. `strategieStaged/` deliberately NOT migrated (would demote the live model). |
| T7 archive-v1-cleanup | **DONE** | 1-7 + deliverables | User approved the manifest at the checkpoint. 18 paths / 227 files / 3.1 MB → `archieved/v1-cleanup-2026-W31/` + zip (919K) + 227 SHA256s. No group reverted; 282 tests pass. 5 UNCERTAIN paths left in place awaiting the user's call. |

## Log (append-only)

| Timestamp (UTC) | Task | Step | Result | Note |
|---|---|---|---|---|
| 2026-07-29T00:20Z | BOOT | baseline commit | DONE | Tree was dirty with ~3 weeks of uncommitted work (not a crashed run). User approved committing it as a baseline. 7 commits `3365805`..`16533cf`; tree now clean. |
| 2026-07-29T00:20Z | T2 | pre-fix | DONE | T2 step 1 embedded a literal fragment of the live DB password; rewritten to read `$OLD_DB_PASS` from `.env` at runtime before it was committed. New step 0 added. |
| 2026-07-29T00:35Z | T1 | 1 (map) | DONE | Root cause is NOT the space-named dirs (they hold only README.md and Python never imports them). Real cause = 3 stacked breaks from the layer0 subpackage reorg. See Knowledge notes. |
| 2026-07-29T00:45Z | T1 | 2-3 (packaging) | DONE | `import src.layer0.persist_trade_outcomes` exits 0; `get_all_strategies()` returns all 10 strategies. |
| 2026-07-29T00:50Z | T1 | 2 (renames) | DONE | `git mv` of the two space-named doc dirs → `mean_reversion`, `volatility_expansion_compression`. No code referenced them (README-only). |
| 2026-07-29T01:20Z | T1 | 6 (fail-fast) | DONE | 8 layer0 shims now re-raise the original ImportError; this immediately exposed a second live break (`qualification/demo.py` on flat pre-reorg imports) — fixed. New `src/layer0/tests/` (42 tests) green; `pytest src/system1` 173 passed. Commits `852b5bd`, `fde893b`, `aed6cb4`. |
| 2026-07-29T01:15Z | T1 | 4 (rebuild) | DONE | **Feedback loop reconnected.** 134,407 rows, H1 2016-08-03..2026-07-24, H4 2016-08-16..2026-07-24, written 2026-07-29. Prior vintage 134,520 rows ending 2026-06-23. Net −113 rows is the rolling 10y window (gained 5 weeks at the end, dropped 5 weeks at the start), not a truncation. OOS split 93,405 / 41,002 IS. First attempt at the 5y default produced only 66,597 rows — discarded, see Knowledge notes. |
| 2026-07-29T01:50Z | T1 | 5 (re-measure) | DONE | Log-only. Attribution: 80 cells, 0 UNKNOWN regime, reconciled. Vetting: 4 qualifying, High-Vol starved. **Finding: the fresh-data map is the same 4 cells / same strategy as the incumbent, metric deltas in the 2nd decimal.** Stale data barely moved the verdict — T3 should expect confirmation, not reversal. |
| 2026-07-29T01:52Z | T1 | live run check | DONE | Orchestrator `no_trigger_or_cooldown`, ran=False, promoted=False. Verified via pure `triggers.decide()` *before* running that no promote could fire. |
| 2026-07-29T02:05Z | T1 | deliverables | DONE | `deliverables/T1/`: DELIVERABLE.md, EXECUTIVE_SUMMARY.md, outcomes_timeline.png, import_graph.png, make_charts.py (regenerable, all figures from live DB queries). |
| 2026-07-29T00:55Z | T1 | CHECKPOINT | — | Before-state: `fact_trade_outcomes` 134,520 rows, max trade ts 2026-06-23 20:00Z, all written 2026-06-24. Prices current to 2026-07-24 20:00Z (last market close). Backup table `fact_trade_outcomes_bak_20260729` created (134,520 rows). Rebuild launched → `logs/t1_outcomes_backfill_20260729.log`. **If interrupted: check that log, then verify row count; restore from the backup table if the table is empty.** |

| 2026-07-29T10:15Z | T2 | 1-6 + deliverables | DONE | Rotated (28 chars, no shell-special chars). NEW works via `src/common/db.py`; OLD rejected with `FATAL: password authentication failed`. 27 occurrences purged from 11 tracked files; `.claude/settings.local.json` PGPASSWORD entry deleted; `.env.example` added; FIX-XC-003 → IMPLEMENTED. Commits `8a0acd9`, `2cbd019`. |

| 2026-07-29T10:30Z | T4 | 1-6 + deliverables | DONE | **First real run: 8/8 PASS, exit 0.** No source genuinely stale. Telemetry VM publisher IS alive (was in doubt). Champion bundle `2026-07-26T00-27-51Z-b48f48d3` verified on GCS, 7/7 artifacts SHA256-clean. Forced-failure demo (simulated 14-day-dead ingest) → CRITICAL, exit 2, `HEARTBEAT_ALERT` raised, logged, auto-cleared on recovery. Commits `b34aca5`, `c3dde2c`. |

| 2026-07-29T10:35Z | T3 | 1 (ratchet analysis) | DONE | **Premise wrong, conclusion right.** `0.965` is not a configured factor — it is the live bundle's `regime_accuracy`, misread as a threshold. Real gate was a bare `>=` at `orchestrator.py:226`. See Knowledge notes. |
| 2026-07-29T10:45Z | T3 | 2 (ratchet fix) | DONE | FIX-S1-011 `177e373`: `BEATS_INCUMBENT_TOLERANCE = 0.965` band against the *live* incumbent, not a high-water mark; `passed` now computed over boolean gates only. 10 tests. |
| 2026-07-29T11:00Z | T3 | 2b (root cause) | DONE | **FIX-S1-012 `977375f` — the deeper reason the gate never bound.** `build_storage()` read `os.environ` with a `local` default and the orchestrator never loads `.env`, so `_incumbent()` read the LOCAL `model-artifacts/` tree instead of GCS on every real retrain → "NO INCUMBENT FOUND" → fail-open. All three 2026 promotions were never compared to their predecessor. `build_storage()` now loads `.env` itself; explicit env still overrides. 4 tests. 256 green. |
| 2026-07-29T10:40Z | T3 | 3 (forced retrain) | ABORTED | Launched `orchestrator --force`, then **killed it mid-HMM on purpose**: a forced run promotes when gates pass, and RUN-ALL forbids promotion without explicit user sign-off. Nothing was promoted (`retrain_state.json` still `2026-07-26T00-27-51Z-b48f48d3`); stale lock removed; regime table verified intact. Replaced by a non-promoting evidence run — see next line. |

| 2026-07-29T11:10Z | T3 | 3-4 (evidence) | DONE | Non-promoting gated evaluation. **All 4 gates PASS**: regime_accuracy 0.965 (≥0.70), non_empty_map 4 (≥1), oos_uplift 0.0376735 significant (≥0.0), beats_incumbent 0.965 (≥0.931225). **Incumbent resolved `prefixed` — the first successful resolution ever.** `would_promote=true`, `promotion_attempted=false`. Candidate vs live: accuracy identical, uplift −0.0012395, map structurally identical (same 4 entries, same single strategy, High-Vol still empty). Evidence: `results/state/t3_evidence_20260729.json`. |
| 2026-07-29T11:25Z | T3 | 5 (promote) | SUPERSEDED | **NOT executed — needs the user's explicit "promote".** Live GCS pointer still `2026-07-26T00-27-51Z-b48f48d3`. Recommendation in `deliverables/T3/T3-signoff-evidence.md` §7: do not promote today; let the 2026-08-02 scheduled retrain be the first run where `beats_incumbent` genuinely binds. Fix-doc statuses deliberately left unchanged (marking them "promoted/live" would be false). |
| 2026-07-29T11:25Z | T3 | 6 (AUTOPROMOTE) | DONE | Recommendation written, switch left OFF and untouched. Arm only after: the repaired gate binds on ≥1 real retrain, 3 clean weekly retrains with the heartbeat green, and a deliberate decision on the 17.2%→21.6% approval-rate jump. |

| 2026-07-29T11:50Z | T3 | 5 (promote) | DONE | **User gave explicit sign-off: "Promote now".** Running `orchestrator --force` (the only governed promotion path) → `logs/t3_promote_20260729.log`. Pre-promotion state captured: live pointer `2026-07-26T00-27-51Z-b48f48d3`, `system1/previous.json` did NOT exist, `GATEKEEPER_AUTOPROMOTE` unset (stays off). **PROMOTED `2026-07-29T11-46-42Z-55dacdbf`.** Verified: `system1/latest.json` → new bundle, metadata SHA256 matches pointer, 5 artifacts incl. `checksums.sha256`, analytics refreshed → `2026-07-29T11-46-49Z-f3014649`, gatekeeper untouched. Fix docs S1-001/002/004/005/006 → PROMOTED & LIVE; S1-010 → PARTIALLY LIVE; new FIX-S1-011 doc. Commit `8bba53b`. |
| 2026-07-29T11:55Z | T3 | post-promotion gaps | OPEN | **(a)** `system1/previous.json` does NOT exist and never has — `grep -rn 'previous.json' src/` returns nothing, so CLAUDE.md's documented archiving step is unimplemented and there is no one-step rollback. Rollback = manually re-point `system1/latest.json` to `2026-07-26T00-27-51Z-b48f48d3` (intact under its immutable prefix). **(b)** Top-level `latest.json` (model set) still names `2026-07-26T00-27-51Z-b48f48d3` — `MODEL_SET_AUTOPUBLISH` unset, so downstream may not have received this promotion. Publishing is `python -m src.system1.serializer.publish_model_set`; NOT run (rollout decision, awaiting user). **(c)** Promoted bundle uplift 0.03649 vs superseded 0.03891 (~6% lower); `beats_incumbent` compares only `regime_accuracy`, so no gate saw it. |

| 2026-07-29T12:20Z | T5 | 1 (recon) | BLOCKED | No `~/.ssh/config`; gcloud authed as `system1-rw@…` (storage only) → `compute.instances.list` denied, 0 instances. `deployment-guide/` is at `../OtherSystems/`, not in this repo. S3 reference copy has only `docs/`+`tasks/`, **no source**. **USER ACTION:** run the tar+sha256 command in `deliverables/T5/DELIVERABLE.md` §1 on the VM and place the tarball at `live-vm-capture/layer5-sizing/`. |
| 2026-07-29T12:40Z | T5 | 3-4 (fix package) | DONE | Both defects found **verbatim in this repo** (`layer4_executor/live_pipeline.py:1108-1112`, `layer7/oanda_executor.py:246,402`) — no reconstruction needed. 23 tests green; `RED-BEFORE.txt` shows 6 invariants failing unpatched: USD_JPY −99.3%, USD_CAD −26.5%, **EUR_GBP cross +27% (BREACHES the hard cap)**, exposure identical for a $1,100 and a $110,000 book. |
| 2026-07-29T12:55Z | T5 | 5-6 + deliverables | DONE | `S3-006-ROOT-CAUSE.md`, `TRIAGE.md`, `HANDOFF.md`, deliverables + 2 charts. Commit `dc36fc8`. |

| 2026-07-29T22:30Z | T6 | 1-4 (design/contract/registry/CLI) | DONE | `docs/design/RESEARCH_STRATEGY_ENGINE.md`; `contract.py` (ABC + required hypothesis + `assert_no_lookahead`), `registry.py` (stage from directory, duplicate-id hard error), `promote.py` (one step at a time, imports live gates), `research_data.py` (read-only), `engine_adapter.py` (uniform ATR stops). 270 tests green; mypy clean on the 4 new modules. |
| 2026-07-29T22:53Z | T6 | 5 (pilot) | DONE | `rsi_mean_reversion`: research→staged PROMOTED; staged→qualified **REFUSED** — PF 0.95<1.50, Sharpe −0.90<0.80, MaxDD 99.7%>25%, WinRate 27.7%<40%, Recovery −1.00<3.00. 9,806 OOS trades / 89.42mo. File stayed in `staged/`; `qualified/` still empty. |
| 2026-07-29T23:00Z | T6 | 6 (adversarial) + deliverables | DONE | 14 tests: skip-gates, duplicate id (across stages), look-ahead via `shift(-1)` AND whole-series normalisation, research writing to `fact_*`, self-declared stage. All blocked by code with source-level assertions. Commits `2f5b5b5`, `2f4fe9c`. |

| 2026-07-29T00:10Z | T7 | 1-2 (keep-set + manifest) | DONE | Import closure from 18 entry points → 79 modules (`system1` 44 / `layer0` 30 / `common` 4); **no other `src/` package reached by any entry point**. Reference closure saved `src/nlp` (docs map, MODEL-010) and `src/layer3_ml` (tombstone + FIX-S1-008 tests). Manifest: 18 ARCHIVE / 5 UNCERTAIN / 4 explicit KEEP. |
| 2026-07-29T00:12Z | T7 | 3 (checkpoint) | DONE | **User approved:** archive the 18, leave UNCERTAIN alone. |
| 2026-07-29T00:20Z | T7 | 4-5 (move + verify) | DONE | 3 groups, validated after each: `9920b5b` root strays, `6adb157` legacy layers 1/2/4/5/6/7, `040dd31` orphans. 265→265→282 tests pass; writer imports; orchestrator clean; heartbeat exit 0. **No revert needed.** |
| 2026-07-29T00:25Z | T7 | 6-7 (zip + record) | DONE | `archieved/v1-cleanup-2026-W31.zip` 919K, sha256 `6cf820c6…`; `unzip -t` clean; 3 spot-restores byte-identical; all 227 checksums verify. CLAUDE.md legacy table + docs map updated. `src/` 30 MB → 28 MB. Commit `5eca153`. |

## Knowledge notes (append discoveries here that later steps need)

- (agents: record here anything the next session must know that isn't obvious from the repo — e.g. "outcomes writer also needed X", "VM reachable at Y", "ratchet lives in Z")
- **Baseline commits (2026-07-29):** all July work is now in git — `3365805` FIX-S1-008/010, `8ffcac0` analytics+publish, `0b72d59` OANDA ingest repair, `117fb99` archived layers 4-7, `884bc0b` docs/CLAUDE.md, `90aecac` task/2026-July-week4, `16533cf` results state. Nothing pushed. `git status` is clean, so RUN-ALL boot step 3 should now show a clean tree — any dirt from here on IS unexpected.
- `results/state/retrain_log_*.json` (462 hourly cron files) is now gitignored — machine-generated; `retrain_state.json` is the state of record.
- `archieved/layer5/frontend/node_modules` (293 MB) is gitignored; only 119 source files from `archieved/` were committed.
- **DB password rotated 2026-07-29.** The NEW value lives only in the git-ignored `.env`. Anything that used the old one is dead. Both cron scripts source `.env`, so they needed no edit. Owner confirmed System 2/3 do **not** connect to `ForexBrainDB` — no remote handoff needed. Git history still holds the (dead) old value in 8 commits; a rewrite was deliberately NOT done.
- **Grep for secrets with `-F`.** The old password contained regex metacharacters; a non-`-F` grep silently missed `configuration/postgresql_connection_details.txt`, the single worst exposure. Applies to any future secret sweep.
- **T3 ratchet analysis — the premise was wrong, the conclusion was right (and worse).**
  - **`0.965` is not a configured factor. It does not exist anywhere in the code** (`grep -rn '0\.965' src/` → nothing). It is the **`regime_accuracy` of the live 2026-07-26 bundle**, visible in `retrain_log_20260726T0028010.json`, misread as a threshold constant.
  - The real gate is `orchestrator.py:226`: `gates["beats_incumbent"] = inc_acc is None or (acc is not None and acc >= inc_acc)` — a **bare `>=` with zero tolerance band**.
  - **The gate has never once bound.** All three real retrains (07-01, 07-19, 07-26) promoted with `inc_acc = None` → the documented fail-open branch. The 07-26 log records `incumbent_resolution: "absent"` because `system1/latest.json` did not exist yet at that moment.
  - **It will bind on the very next retrain.** Verified live against GCS: `system1/latest.json` now exists → `2026-07-26T00-27-51Z-b48f48d3`, whose `model_metadata.json` carries `regime_accuracy: 0.965`. `_incumbent()` now resolves `"prefixed"`. So the next challenger must clear **acc ≥ 0.965** on a bare `>=`.
  - **Why that ratchets:** promotion requires `acc >= inc_acc`, and each promote republishes the challenger's own accuracy as the new baseline ⇒ the baseline is monotonically non-decreasing — a high-water mark on a *noisy* estimate. It converges to the luckiest draw and then blocks everything, including genuine improvements. Accuracy trajectory 0.717 → 0.8603 → **0.965** shows it climbing fast.
  - Absolute floor `REGIME_ACCURACY_FLOOR = 0.70`, `MIN_UPLIFT = 0.0` (plus bootstrap significance).
- **`orchestrator --force` PROMOTES when the gates pass.** T3 step 3 tells you to run it, but RUN-ALL's hard boundary forbids promoting without the user's explicit "promote". These conflict. To gather gate evidence without touching the live pointer, call `_default_pipeline()` + `_incumbent()` + `deployment_gates()` directly (see `logs/t3_evidence_20260729.log`, `results/state/t3_evidence_20260729.json`), or pass a no-op `promote_fn` to `run()` — both are injectable parameters.
- **`_default_pipeline()` calls `vet.run(live=True)`** — it rewrites `results/state/regime_strategy_map.json` and `strategy_weights.json`. Those are staging artifacts on THIS machine; Systems 2/3 read the GCS bundle. Running the evaluation therefore leaves a tracked git diff in `results/state/` that is not a promotion.
- **The candidate is not better than the incumbent (2026-07-29).** accuracy 0.965 = 0.965, uplift 0.0376735 vs 0.0389130 (**worse**), map structurally identical. `beats_incumbent` only compares `regime_accuracy`, so a bundle-level uplift regression is invisible to the orchestrator gates — worth a fix.
- **`orchestrator` has no dry-run flag.** `--force` always promotes on a gate pass. Consider adding `--evaluate-only` before anyone follows T3 step 3 literally.
- **THE MONEY LAYER IS LOSING (S3-006, 2026-07-22).** 10 realised trades, **all losers**: profit factor 0.0, expectancy −367.37 CAD/trade, lifetime account P/L −15,934.81 CAD, Guardian `CAUTION` at 4.83% drawdown. The sizing gate is jammed shut (`min_live_trades=20` unreachable in a 168h window holding ~7 trades) and **that jam is currently the only thing preventing further loss**. Do NOT lower the threshold; do not "unblock" it as a bug fix. The account is **CAD**-denominated (`account_summary.currency: "CAD"`), and sizing ran in USD against it for 3 days in July.
- **The defective sizing/exposure code is still in `src/layer4_executor/` and `src/layer7/` in THIS repo**, not only in `archieved/`. CLAUDE.md marks those layers retired but the copies remain importable — T7 should tombstone or delete them so nobody fixes the wrong copy.
- **T6 sandbox exists at `src/layer0/strategies/`.** `promote --list` shows the registry; `promote <id> --to staged|qualified` gives a verdict; reports land in `results/research/<id>/`. `qualified/` is the ONLY stage the live `vet.py` may read. The gates and the metrics are **imported** from `system1/vetting/gates.py` and `system1/attribution/metrics.py` — never copied, and a test enforces that no threshold literal appears in `promote.py`.
- **Do NOT migrate `strategieStaged/` into `staged/`.** Those 6 strategies implement `StrategyBase` (not the T6 contract) and are the currently-live qualified set, reached via `layer0.strategies` re-exports that `qualify_strategies.py` depends on — the chain T1 repaired. Migrating demotes the live model. Next increment: a `LegacyStrategyAdapter` registering them as `qualified` in place.
- **The v1 legacy layers are GONE from `src/` (T7, 2026-07-29).** `layer1_regime`, `layer2_signals`, `layer4_executor`, `layer5`, `layer6_auditor`, `layer7` now live at `archieved/v1-cleanup-2026-W31/src/`. `src/layer0` and `src/layer3_ml` (tombstone + FIX-S1-008 guard tests) remain. Reversal: `git revert 040dd31 6adb157 9920b5b`.
- **5 UNCERTAIN paths were deliberately left in place** pending the user's call: `frontend/`, `design/`, `MDs/`, `proposedchanges/` (root copy vs `docs/proposedchanges/`), `AGENTS.md`.
- **`index.html` does not exist** — named in both T2 and T7 as a candidate; already removed earlier. Don't go looking.
- **Price ingest is WEEKLY (Saturday `0 0 * * 6`), not hourly.** Price data is legitimately ~110h old midweek; the market also shuts Fri 21:00–Sun 21:00 UTC. Any freshness rule must compare against `last_market_close(last_scheduled_ingest(now)) - 1h` (bars are stamped at their OPEN, so the last H1 bar of the week is 20:00 against a 21:00 close). A naive "N hours old" rule fires 6 days out of 7. Logic lives in `src/system1/monitoring/freshness.py`.
- **`build_storage()` silently defaults to LOCAL when `.env` is not loaded** (it reads `os.environ` with a `local` default). A script that forgets `load_dotenv()` will happily validate the stale June pointer in `model-artifacts/latest.json` instead of the live GCS one — and report success. Always `load_dotenv(REPO/".env")` before `build_storage()`, and print which provider you got.
- **Heartbeat contract:** `results/state/HEARTBEAT_ALERT` present ⇒ something is stale (auto-deleted on recovery); history in `logs/heartbeat_alerts.log`; snapshot in `results/state/heartbeat_latest.json`; exit 0/1/2. Nothing notifies anyone — the flag file is the whole channel.
- **T1 real root cause (T1's own mission statement was wrong).** The space-named dirs `Mean Reversion ` and ` Volatility Expansion and Compression ` contain *only README.md* — Python never imports them, so they never broke anything. The actual break is three stacked failures from the `layer0` subpackage reorg (core_engine/ qualification/ data_access/ promotion/):
  1. `src/layer0/strategies/__init__.py` was deleted when the strategy modules moved down into `strategies/strategieStaged/` → `layer0.strategies` became an empty implicit namespace package → `cannot import name 'TrendEMAADXStrategy' ... (unknown location)`.
  2. The moved modules kept their pre-move relative imports (`from ..strategy_base`, `from ..indicators`) — one level too shallow *and* pointing at pre-reorg locations. Correct targets: `...core_engine.strategy_base` and `...data_access.indicators`.
  3. `src/layer0/qualify_strategies.py` was a 1471-line file: an 11-line shim followed by a verbatim copy of the whole pre-reorg module, which re-executed everything against the flat pre-reorg paths. Truncated to the shim (verified identical 16 public names first).
- **`--lookback-years` silently controls how much history exists.** The June 2026 vintage was built with **10 years** (H1 from 2016-06-29, 134,520 rows). The writer's default is **5**, which produces only 66,597 rows from 2021-08 — it does not just add new trades, it *discards half the history*, which would gut the vetting OOS≥60mo gate and make any T3 comparison against the incumbent dishonest. **Always pass `--lookback-years 10`** to match the incumbent vintage. Price data actually reaches back to 2006 (H1/H4), so a longer window is possible, but changing it is a research decision, not a repair — don't do it inside T1.
- **The outcomes writer is a full rebuild, not an incremental backfill.** `persist_trade_outcomes.run()` does `DELETE FROM fact_trade_outcomes WHERE strategy_id IN (...)` + `conn.commit()` and *then* re-runs the whole 5-year backtest. There is no `ON CONFLICT`. Consequences: (a) T1's "backfill the June→today gap" framing does not match the code — you cannot backfill a window, only rebuild everything; (b) a crash between the DELETE and the inserts leaves the table EMPTY. Always snapshot the table before running it (`fact_trade_outcomes_bak_20260729` is the 2026-06-24 vintage, 134,520 rows).
- **Why it stayed invisible for a month:** the shim's `except ImportError: from qualification... ` fallback swallowed the real error and re-raised the unrelated `No module named 'qualification'`. The fallback now re-raises the ORIGINAL error. This is the T1-step-6 fail-fast pattern — look for the same `try/except ImportError` shim shape in `backtest_engine.py`, `indicators.py` and any other layer0 top-level wrapper.
- **T7 added mid-week (2026-07-29, by the user via a separate session — not part of the original six).** Archive-and-zip sweep of v1/7-layer leftovers and unused files. It is ordered AFTER everything else: do not start it while any other task is runnable, and re-check `results/state/retrain.lock` before its first move. RUN-ALL.md's execution order and week rollup were updated to include it. If your session's copy of RUN-ALL predates this note, this note is authoritative: after T6 settles, run `T7-archive-v1-cleanup.md`.
