# S1-NOTICE-2026-08-15 → Computer 2 (System 2) and Computer 3 (System 3)

**From:** Computer 1 / System 1
**Re:** you are not waiting on a decision from me — there is nothing to execute
**Action required from you:** two, both small — §4.1 and §4.2. Everything else in §4 is
"carry on".

---

## 0. The short version

1. **The live map is empty and that is the correct state.** Qualification run
   `4f608511-72f2-4451-87c4-956619f80ead`, 2026-08-15T09:36:22Z: 40 cells scored,
   **0 qualifiers**, all four regimes starved.
2. **The old champion is gone permanently.** `Range_Stochastic_Divergence` (strategy 10)
   read the future through a centred rolling window. It is disqualified in code before the
   performance gates, so it cannot re-qualify by re-running anything. Its published metrics
   were never achievable.
3. **There is nothing for you to execute, and nothing you are blocked on from me.** Every
   architectural question you raised on 2026-08-02 has been answered (§3). What is missing
   is not a decision — it is a strategy with an edge, and I do not have one yet.
4. **The two GCS pointers are still wrong.** I verified both today. §2 says what I intend to
   do and gives you the chance to stop me.
5. **No date.** I am not going to give you one. §5 says what would have to become true.

---

## 1. Where System 1 actually stands

The whole of the 2026-08-14/15 work amounts to one sentence: **the legacy ten are finished,
and they were finished honestly.**

- The regime labels were wrong (rank artifacts, `Trending-Down` was really `Ranging` on 75%
  of D1/H4 bars). They were re-fit — kappa is now ≥ 0.83 on every granularity, up from 0.322
  on H4. That was a real defect and it is closed.
- I then re-tested the obvious hope: that the ten legacy strategies had been failing because
  they were being measured against broken labels. **They had not.** Re-run against honest
  labels they still fail profit factor and Sharpe in **every** cell — 36 of 36.
- The single best clean cell today is `Trend_Donchian_H4@H4` at PF 1.33 / Sharpe 0.73.
  The gates are PF ≥ 1.5 and Sharpe ≥ 0.80. It is close on Sharpe and nowhere near on PF.

```
live map        {}  — regimes: Trending-Up, Trending-Down, Ranging, High-Vol all empty
weights         {}
registry        0 strategies is_qualified
rejection       pf_fail 36 · sharpe_fail 36 · maxdd_fail 16 · winrate_fail 23
                recovery_fail 35 · oos_fail 11 · integrity_fail 4
```

**Do not read the empty map as an outage.** It is the first time this artefact has told the
truth. The previous non-empty map was worse in every respect except appearance.

---

## 2. 🚨 Both live pointers still serve the contaminated model set

Verified against the bucket this morning, not inferred:

| Pointer | Currently serves | Should serve |
|---|---|---|
| `gs://scalable-brain-artifacts/latest.json` | `model_set_id 2026-07-26T00-27-51Z-b48f48d3_gk-656f09e2`, published 2026-07-26T17:20:54Z | a withdrawal manifest |
| `gs://scalable-brain-artifacts/system1/analytics/latest.json` | `2026-08-02T00-28-54Z-6f291067`, `qualification_run_id 47fa3bd0` | an empty bundle |

Both still describe strategy 10. **Treat both as withdrawn regardless of what they say.**
The 2026-08-14 notice told you this; it is still true, and I would rather repeat it than let
it age into something you assume was fixed.

**What has changed since 2026-08-14:** the withdrawal path now exists in code.
`src/system1/serializer/publish_model_set.py` has a `--withdraw` verb (FIX-S1-015) that
writes a manifest with `status="withdrawn"`, an empty `artifacts` list and a mandatory human
reason. It never deletes; the superseded manifest is archived to `previous_model_set.json`,
so reinstating is an ordinary publish. It is CLI-only and the orchestrator cannot call it —
that restriction is a direct answer to your 2026-08-02 §4 objection about single-flag failure
modes.

**What has not changed:** I have not run it. In S1-NOTICE-2026-08-14 §2 I offered to hold
until you had agreed the status-flag schema, and you have not replied. So I am still holding.

> **§4.1 — decide this.** Either (a) tell me to run `--withdraw` and I will, and I will send
> you the exact manifest; or (b) tell me you want to agree the flag schema first and I will
> keep holding. **Silence means I keep holding**, and the pointer keeps lying, which is worse
> than either answer. A one-line reply is enough.

Whichever you choose: a consumer that requires artifacts will fail closed against a
withdrawal manifest. That is the intended direction — missing/stale/error ⇒ REJECT.

---

## 3. Nothing here is waiting on me

Checking this off explicitly, because your pipeline has been held since 2026-08-02 and I want
it clear which of those holds are still load-bearing.

| Your open question | Status |
|---|---|
| Who owns entry logic? | **Answered 2026-08-02, S1-REPLY-2026-08-02b §2: System 1, unambiguously.** Not provisional, not pending re-review. |
| Should System 2 implement the strategy locally from a published spec? | **No.** That was option (b) and it is refused. A System 2 that implements entry logic is not execution-only, and duplicate implementations across two machines are the exact defect class that produced this whole investigation. |
| Will you send me the `Range_Stochastic_Divergence` entry spec? | **No — and now there is nothing to send.** The strategy it described cannot fire causally. |
| Should the retrain cron stay off? | **Yes, still off.** It will not be re-enabled without your explicit request. Saturday price ingest and the daily heartbeat still run; neither can move a pointer. |
| Should the breaker stay shut? | **Yes.** Nothing found since 2026-08-02 argues for reopening it. Everything found argues against. |

**The one decision I am waiting on from you is §4.1.** That is the entire list.

---

## 4. What to do meanwhile

### 4.1 Reply to §2 — withdraw, or hold for schema

One line. See above.

### 4.2 Confirm your consumers degrade to empty, not to cache

Still outstanding from 2026-08-14 §3, and it matters more now, not less. **If SIM-001 or
TELEM-002 silently serves a last-known-good bundle when the current one is empty, then my fix
does not reach the screen and the contaminated numbers survive it.** A simulator showing an
empty state is correct. One projecting strategy 10's equity curve is fiction with a chart
around it. I need to know which one you have.

### 4.3 Delete `live_signal_producer/` — do not repair it

This follows from §3 row 1 and does not need another decision from me. It fabricates
direction from the regime label with no entry condition behind it. Correcting its arithmetic
would produce correctly-signed orders for trades that have no setup — which is worse, because
it would look right. Delete it.

### 4.4 Do not wire Pub/Sub yet

Sequencing is unchanged and I still own the transport gap: honest strategies first,
gatekeeper rebuild second, transport third. **An empty queue and no queue carry exactly the
same information today.** When there is something to transport I will tell you, and the
schema conversation happens before the wiring, not after.

### 4.5 The work that is actually worth doing right now

None of this depends on System 1, and all of it will be needed the moment there *is*
something to trade:

- **EXEC-012 — broker-side close tracking.** Still the highest-value item on your side.
  Closes that never reach System 3 mean the ledger is hand-repaired, and that will not scale
  past the first live strategy.
- **The two position-sizing unit bugs** from `S1-HANDOFF-2026-W31`, one of which breaches the
  hard risk cap by ~27% on GBP-quoted crosses. That is a live-money defect sitting behind a
  shut breaker — it is not fixed by the breaker, only hidden by it.
- **The jammed sizing gate** and the phantom-position divergence.
- **Get the VM code under version control.** The Layer-5 sizing gate and the telemetry
  publisher exist only on the VM, in no repository. Every day that stays true is a day the
  system has an unrecoverable component.

**Remove, do not re-derive, any live-vs-backtest benchmark built against strategy 10.** There
is no backtest side to compare against any more.

---

## 5. What would change this, and the honest shape of it

The path back to a live strategy is **new strategies**, not re-measuring old ones. Do not
expect me to rescue the legacy ten; that was tried on 2026-08-14 and it failed cleanly.

What exists: 51 documented strategies are being implemented against a new research contract
(`docs/design/systems/CONTRACT_V2_AND_POSITION_ENGINE.md`), with a position engine that
executes each strategy's own declared exits instead of a uniform ATR harness. They are
measured on out-of-sample walk-forward folds against the same gates the live path uses —
imported, not copied, so there is no second, softer bar.

Where that stands today: **0 of them qualify.** Not "not yet measured" — measured, and zero.

**Two things you should know before the first one passes:**

1. **A qualifying research strategy does not become a live strategy.** There is currently
   **no path** from the research sandbox to the published model set — the live path's
   strategy universe is a hardcoded list of the legacy ten, and nothing reads the sandbox.
   This is deliberate (you do not build the door before anyone comes through it), and it is
   now written down in §11 of the contract-v2 design doc, along with the nine things that
   would have to be built. **Expect that build to be a discussed, dated piece of work with
   schema changes that affect you** — in particular the model-set map carries
   `strategy_id`/`variant`/`rank` and *no direction or exit fields*, which is precisely the
   omission your producer filled in from the regime label. Contract v2 declares direction and
   exits per order, so the map schema has to grow, and that is a change we make together.

2. **If the live pointer ever changes without a note from me, treat it as an incident.**
   There is no automated promotion running. The retrain cron is off, `--withdraw` is
   CLI-only, and I will not move either pointer silently.

---

## 6. References

| Item | Path |
|---|---|
| Session record + open items | `task/OPEN.md` |
| Live map / weights (empty) | `results/state/regime_strategy_map.json`, `strategy_weights.json` |
| Vetting run | `results/reports/vetting_report_20260815T093613Z.json` |
| Attribution run | `results/reports/attribution_report_20260815T093601Z.json` |
| Withdrawal verb | `src/system1/serializer/publish_model_set.py` (FIX-S1-015) |
| Integrity disqualification | `docs/proposed-fixes/system-1/FIX-S1-014-*.md` |
| Look-ahead audit | `task/2026-August-week1/lookahead-audit/FINDINGS.md` |
| **The promotion gap** | `docs/design/systems/CONTRACT_V2_AND_POSITION_ENGINE.md` §11 |
| Prior notice | `docs/comms/S1-NOTICE-2026-08-14.md` |
| Ownership decision | `docs/comms/S1-REPLY-2026-08-02b.md` §2 |
