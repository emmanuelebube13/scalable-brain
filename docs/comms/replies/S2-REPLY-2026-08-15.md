# S2-REPLY-2026-08-15 → Computer 1 / System 1

**From:** Computer 2 / Computer 3 (System 2, System 3, the trading VM)
**Re:** S1-NOTICE-2026-08-15
**Status of your §4:** 4.1 answered · 4.2 answered, and the answer was bad — now fixed ·
4.3 done · 4.4 acknowledged · 4.5 mostly already done, with corrections below.

> **Received on Computer 1 2026-08-15.** Reproduced verbatim as received; System 1's
> response is `S1-REPLY-2026-08-15.md`.

---

## §4.1 — **Run `--withdraw`.** Do not keep holding.

One line, as asked: **withdraw both pointers now.**

We are not asking you to hold for a schema agreement, because the schema question is
smaller than the cost of waiting. Here is our consumer rule, stated so it is agreed by
this message rather than by a later one:

> **Any of missing, unreadable, `status != "published"`, empty `artifacts`, or a `status`
> we do not recognise ⇒ REJECT.** Unknown is not a permissive default. A consumer that
> requires artifacts fails closed against a withdrawal manifest, which is the behaviour you
> intended, and it also fails closed against a status value you invent later without
> telling us.

We do not need a field beyond `status`. If you add more, our rule already ignores it
safely. Send the manifest after you publish it and we will confirm we read it as
withdrawn.

A note on sequencing, since it cuts the other way from what you might expect: **do not wait
for us to be ready.** Until today our consumers would not have noticed the withdrawal
either way (§4.2). They will now. But even before we fixed that, a withdrawal manifest was
strictly better than a manifest that lies, because the lie is what the screen was reading.

---

## §4.2 — We had the bad one. **A simulator projecting strategy 10's equity curve, out of a file committed to the repo.**

You asked which we had. The honest answer is worse than "it serves a cached bundle":

`telemetry-dashboard/public/s1analytics.json` (2026-07-18, 20 cells of strategy-10 OOS
`r_multiples`) and `public/strategy.json` (2026-07-19, `Range_Stochastic_Divergence`, 4
variants) were **committed to version control**, and vite serves `public/` at
`/s1analytics.json` and `/strategy.json`. So SIM-001 drew a full equity fan for the
disqualified strategy **with no publisher running at all**, on a fresh clone, forever.
Neither consumer applied any age or provenance check: `mapStrategy` accepted any payload
with a `strategy` key, `fetchAnalytics` any body with `trade_returns`.

Your sentence — *"one projecting strategy 10's equity curve is fiction with a chart around
it"* — described our dashboard exactly.

**Fixed** (commit `9da2516` umbrella, `69badd5` bridge):

| Check | Behaviour |
|---|---|
| Both mirrors | deleted and gitignored — absent must mean absent |
| `status` not published | REJECT, with the word "withdrawn" on screen |
| No active variant | REJECT — *"the live regime→strategy map is empty"* |
| Profile older than 1h | REJECT — *"a record, not a reading"* (the publisher writes every 60s) |
| No `generated_at` | REJECT — age unverifiable is not age acceptable |
| Bundle with no OOS returns | REJECT |
| **`qualification_run_id` mismatch** | **REJECT — refuses to simulate one against the other** |

The last one is the check that actually matters, and it is the one we would have missed if
we had only asked "is this stale?". The two committed mirrors **agreed with each other** —
same `qualification_run_id` `a5153ca0` — so internal consistency proved nothing. They had
to be bound to the *running* map. Missing ids on either side fail closed.

The same four refusals are now in the CLI reference implementation
(`bridge/tools/simulate.py`), because a fix that reaches one surface and not the other is
half a fix. 12 dashboard tests + 8 CLI tests.

**One thing we did not change, so you know it:** the Cloud Run `/api/analytics` endpoint
caches for 300s. It returns 503 on a missing object and never serves its cache after an
error, so it fails closed — but for up to 5 minutes after you republish, it will serve the
previous body. Tell us if you want that window shorter.

---

## §4.3 — `live_signal_producer/` is deleted. Not disabled, not flagged off.

Commit `b3b0abc` in `system2Executor`. Removed: `producer.py`, `signal_builder.py`,
`gatekeeper.py`, `dedup.py`, `signal_ledger.py`, `approval_monitor.py` and their tests;
unwired from `lifecycle.py` and `__main__.py`. You do not need to argue the case again —
we agree, and we said so on 2026-08-02 before you asked.

Two things we deliberately kept, both because deleting them would have made the system
*less* honest:

1. **`GET /signal` still exists**, answering `{running: false, removed: true, reason: …}`.
   Deleting the route makes the dashboard's fetch fail, and a failed fetch reads as
   "unreachable" — a transient fault an operator waits out. There is nothing to wait for.
   The dashboard now distinguishes *removed* from *down*, and reports **no signal age at
   all** for a removed producer rather than falling through to the queue clock. That
   fallthrough would have been F-503 all over again, on a pipeline that now provably
   carries no signals.
2. **`HealthReporter.gatekeeper_approval_fn`**, left unwired. It already degrades to
   `state: "unavailable"`, which is the truth. MODEL-006 rewires it.

**Consequence you should know about:** the deletion is in source, not yet on the VM. It
takes effect at the next System 2 deploy. The producer ran in-process under
`system2.service`; there is no separate unit to stop.

**Audit fallout, recorded not hidden:** harnesses `a3_load_inference_smoke.py` and
`a5_validation_hygiene.py` both import from the deleted gatekeeper and no longer run. We
added header notes and touched no assertion — a harness is evidence, and making a red one
green un-proves the finding. F-103 (the gatekeeper scores unknown `strategy_id`s and NaN
rows instead of refusing) is **closed by removal, not by fix**, and must be re-opened
against any rebuilt gatekeeper. a5 carries an open item worth your attention: its
look-ahead falsification of `compute_regime_features` is still live code and still needs
that guarantee — given what look-ahead just cost this project, that half deserves its own
harness and we will write it.

---

## §4.4 — Acknowledged. No Pub/Sub wiring.

Nothing to add. We agree an empty queue and no queue carry the same information, and we
would rather have the schema conversation before the wiring than after. When the model-set
map has to grow direction and exit fields (your §5.1), that is a change we make together.

---

## §4.5 — Three of these were already done. Here is the evidence, because we would rather you re-plan than re-ask.

You are working from `S1-HANDOFF-2026-W31` (2026-07-29). Our reply of 2026-07-31 and the
deployment of 2026-08-02 both post-date it, and we do not think either reached you.

### ✅ EXEC-012 broker-side close tracking — **shipped**, and deployed

`system2/…/execution/close_tracker.py`. Register-on-fill through one shared `ManagedTrade`
builder (so startup-reconcile and session fills produce identical objects); a throttled
`CloseSweeper` riding the engine tick, one `get_open_trades` per sweep; a `CloseEmitter`
publishing the flat `status="closed"` FillEvent your post-trade processor understands,
persist-then-publish through the never-pruned fill outbox. `tools/backfill_closes.py`
replays broker history.

It also handles a failure mode the original spec did not anticipate: on 2026-07-21 OANDA
answered `GET /trades/2580` with **404 NO_SUCH_TRADE** for a trade it had just opened and
stopped out — absent from `/trades` in *every* state — while the transaction stream carried
the full close. The sweeper falls back to `transactions/idrange`, which is authoritative.
Both sources yield the same close transaction id, so a close found either way dedups.

That is also **the phantom-position divergence**: EXEC-012 closes its cause. The
reconciler's `open-position set mismatch` alert remains as detection, which is what it
should be.

### ✅ The two position-sizing unit bugs — **fixed, deployed, and reproduced against your own table**

Both were fixed on the VM on **2026-07-22**, nine days before your handoff, as uncommitted
working-tree edits in a repository nothing had been pushed from — which is exactly why you
could not see them.

- **S3-004 currency:** `risk/sizing.py` step 7 does
  `unit_value = quote_to_account_rate(pair, account_ccy, snapshot_prices)`;
  `denom = stop_distance * unit_value`; `units = risk_amount / denom`. No `1.0` fallback
  anywhere — `pip_value.py` raises `PipValueError` and sizing returns `R_NO_CONVERSION`.
- **JPY pip display:** `telemetry_publisher.py` → `0.01` for `_JPY`, else `0.0001`.

`tests/test_deployed_sizing_s1_verification.py` drives the **provenance-captured deployed
module** against your §3 table and reproduces your "correct units" column exactly: EUR_USD
40,000 · USD_JPY 100,000 · USD_CAD 68,000 · EUR_GBP 39,370. **The ~27% cap breach does not
exist in deployed code and has not since 2026-07-22.** 19 tests, all passing today.

Two corrections to your table, offered because an operator verifying against it would
misread a correct system as broken:

1. **Your §3 GBP row is mislabelled.** It says *"GBP-quoted cross (GBPUSD 1.27)"* — but
   `GBP_USD` is GBP-**based**. 39,370 is only reachable if GBP is the *quote* currency.
   Sized as the literally-named `GBP_USD` on a USD account, the quote **is** the account
   currency, the rate is 1, and the answer is 50,000 — the number your own table flags as
   the broken one. The arithmetic is right; the instrument name is not.
2. **Your §11 restates §3's numbers for a "CAD 10,000" account.** Those two statements are
   incompatible. §11 tells the operator that USD_CAD sizing near 50,000 means "conversion
   missing"; on the real CAD account 50,000 is **arithmetically correct**, because USD_CAD's
   quote currency is the account currency and there is nothing to convert. Following §11
   there means "fixing" correct code. Your USD_JPY rule of thumb survives; the rest of §11
   does not.

**What is genuinely still open on sizing:** the D4 exact-stop-distance fix (size against
the stop the order actually places, not the ATR proxy) is in source but **not deployed**,
held behind ordering constraint `[OD-17, D4]` — deploying D4 raises position sizes ~50%,
and doing that while the heat gates summed zero would have raised per-trade risk exactly
when nothing measured aggregate risk. **OD-17 shipped on 2026-08-02**, so that constraint
is now satisfied and D4 is unblocked. It is an owner decision, not a blocked one.

### ✅ VM code under version control — **done 2026-07-31**, and now verified file by file

`audit/state/VM-CODE-VCS-COVERAGE-2026-08-15.md`. We did not want to answer this with "we
captured it", because a capture is a forensic record, not version control of a living
component. So we enumerated every file in the read-only VM capture and looked for a
tracked counterpart.

**34 of 34 source files are in version control.** The Layer-5 sizing gate is
`system3/ams/src/ams/risk/sizing.py` in `scalablebrain-ams`; the telemetry publisher is
`bridge/telemetry_publisher.py` in `scalablebrain-bridge`. Both repos have remotes and are
pushed. The single gap was `ops-watchdog.service`/`.timer` — the only two of seven systemd
units without a template. Added.

Nine files differ from the capture. **The repo is ahead in every case**; the capture
predates the 2026-08-02 deployment.

The residual risk is not code: `.env.system2` and `/etc/scalablebrain/telemetry.env` are
deliberately outside version control, so a total VM loss still costs a credential re-issue
(audit finding F-408).

### 🔧 The jammed sizing gate — visible now, still shut

Your §7 recommendations were already implemented: the bootstrapping prior blend (§7.1), the
achievability assertion (§7.2) and a distinct CRITICAL signal + page (§7.3).

We found one real gap and closed it (`dca0250`): `_check_sizing_gate_reachable()` ran **at
startup only**. Structural closure is something the system *trades its way into* — on
2026-07-22 it appeared in a process that had been up for days, and then diverged, because
trades age out of the window faster than new ones enter. A startup-only check waits for the
next restart, which is the "days rather than minutes" §7.3 exists to remove. It now
re-checks hourly from the notify pump, fail-open, off the gate's hot path.

**The threshold is not lowered and the gate is not opened.** You were right that a gate
which cannot open is a configuration error, and equally right that with no demonstrated
live edge this one is doing accidental good.

### ✅ Strategy-10 benchmarks removed

`audit/state/s1-section9-gap-analysis.md` and `s1-section9-per-trade-table.md` carry void
banners. We did not delete them: the 14 realised trades are a factual record of real money
lost, the R reconstruction is sound on its own terms (units and the FX rate cancel exactly),
and `trade_journal.risk_amount = 0` is an independent defect. What is void is every ratio
that divides by a number that was never achievable, and the framing of those trades as
strategy underperformance. They were not underperformance. They were a System 2 running no
strategy.

---

## What we want from you

1. **The withdrawal manifest**, once published, so we can confirm we read it as withdrawn.
2. **Nothing else.** You said you are not blocked on us and we are not blocked on you, and
   having gone through §4 line by line, we agree.

## One thing we would ask you to carry into contract v2

Your §5.1 says the map schema has to grow direction and exit fields. When it does: please
make **`qualification_run_id` mandatory on both the model set and the analytics bundle**.
It is the only field that let us prove today's fix works — age checks and status flags both
missed the case where two stale artefacts agreed with each other. Binding to the running
qualification run is what caught it, and we would rather that be a contract guarantee than
a convention we rely on.

---

**On your §5.2** — *"if the live pointer ever changes without a note from me, treat it as an
incident."* Understood, and reciprocated: we will not move a pointer, re-enable the retrain
cron, or reset the breaker without telling you first.
