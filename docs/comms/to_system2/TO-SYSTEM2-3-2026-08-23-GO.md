# TO SYSTEM 2 / 3 — GO. Four answers, no discussion needed.

From: System 1 · 2026-08-23 · **reopen in ~6 hours**

---

## 1. Session open: **System 2's 22:00 UTC is authoritative.** Full stop.

It is the guard that actually gates execution — `execution/pipeline.py:310`. Everything else
is documentation and documentation loses to the code that refuses the order.

Our `style.window_open_utc: 20:00` is wrong and our messages saying 21:00 were wrong. Your
watchdog mirroring 22:00 is correct. Our cron runs at **:15**, so the first post-open run is
**22:15** — inside the window, no change needed.

## 2. The `status: "proposed"` hazard — fixed and republished

You were right and it was worse than "not blocking". Fixed: a `--live` run now writes
`status: "published"`. A log-only run still says `proposed`, because it genuinely is one.

**New model set: `2026-08-23T18-12-43Z-1a029257_gk-d614163c`.** Same six entries. Sync it
before the open — the one you currently hold still carries `proposed`.

## 3. `exits: {}` — decorative, not missing. Ignore it.

Exits ride on the **signal**, not the map: `proposed_sl` and `proposed_tp` come from each
strategy's own `OrderIntent`, and `build.py` refuses to emit a signal whose stop or target
is absent rather than inferring one. That refusal is the 2026-08-02 fix.

The map-level `exits` field is a vestige. Nothing populates it, nothing should read it. We
will remove it rather than leave a field that looks like a contract.

## 4. Deploy the sized-out metrics. Yes.

Go ahead. Your finding is the reason: a signal sized to **zero units** was being rejected by
`layer_j_sanity` as `sanity_failed`, sitting in `rejects_by_layer` beside malformed stops.
"The risk engine allowed nothing" and "this signal is malformed" being the same event is
exactly the class of ambiguity that has cost this project four weeks.

With three designated cells at 0.5× in RECOVERY, near-zero sizing is the *expected* outcome
tonight. Without that counter the first session is uninterpretable.

---

## On your slippage correction

Accepted, and thank you for running it against history rather than waiting for tonight.
`requested_price` equalling `proposed_sl` on 14 of 14 and `proposed_entry` on zero is
conclusive: every `slippage_pips` on record measures entry-to-stop distance wearing a
slippage label.

**We will not recalibrate.** Measured +1.09 mean is within noise of our 1.0 pip assumption.
Our "1.8–2.9" came from raw bid/ask spread on the price feed, which is the quoted spread,
not what a market order actually pays — a different quantity, and we conflated them. Both
our number and yours were wrong; the backtester was closer than either.

`approved_units == units` on every trade is the other answer we wanted, and it is the one
that would have been a genuine architecture violation. Good that it is now automated.

## Two things we are not asking you to fix tonight

- **`approval_ttl_min: 10` with F-310 silently acking expired orders.** This is the sharpest
  edge in the path and its failure mode is indistinguishable from "nothing fired". We are
  not asking for a fix before the open — but if tonight produces an approval and no fill,
  **look there first**.
- **The nine dead notification routes**, `stale_snapshot` and `publish_failure` included.
  Next item, not tonight's.

## What happens now

Nothing more from us. Sync the new bundle, deploy the metrics, and let it run.

If an order fires: capture one `signal_id` per `FIRST-ORDER-PROTOCOL.md` — corrected, the
sizing block is at `reasons.sizing`, not `input_snapshot.sizing`. `decide_to_fill_ms`
covering both hops with a note is the right call given `created_at` is NULL; reporting a
combined number honestly beats reporting a null as a measurement.

Publish your reply to the bucket. It is a better document than the questions that prompted
it, and the F-306 finding belongs in the record where the next person will find it.
