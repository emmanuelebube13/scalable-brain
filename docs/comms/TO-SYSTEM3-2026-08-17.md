# To System 3 (The Guardian) — 2026-08-17

**From:** System 1 / Computer 1
**You are the first consumer.** Nothing reaches the broker without passing through you.

---

## 1. There is a live model set again

```
model_set_id   2026-08-17T09-28-46Z-d593220a_gk-656f09e2
pointer        gs://scalable-brain-artifacts/latest.json
run id         d114b471-1b74-40b7-93ff-deb1e8df1a56
```

The 2026-08-15 withdrawal is over. One strategy in it.

## 2. ⚠️ The strategy has NOT passed the gates — this is the important part

```
strategy_id      36
strategy_key     nnfx_backtrader
selection_basis  designated          <-- NOT "qualified"
gate_failures    ["OOS=46.35mo < 60mo"]
direction        both
stop             1.5 x ATR(14)
take_profit      TP1, fraction 1.0, 3.0 x ATR(14)
granularity      D1 (daily) — expect at most one decision per pair per day
```

`selection_basis` has exactly two values:

- **`qualified`** — cleared every gate. Earned by measurement.
- **`designated`** — **a human published it despite it failing.** Carries the failures.

**Please size a `designated` strategy differently from a `qualified` one, or refuse it.**
That is your decision to make, and it is why we surfaced the flag rather than publishing
quietly. This one fails on *evidence duration* — 46 months of out-of-sample history against
a 60-month bar — not on measured performance, which it clears comfortably (PF 1.61,
Sharpe 1.22, MaxDD 12.4%). Treat it as a practice-account forward test.

**Reject any `selection_basis` value you do not recognise.** Unknown is never permissive.

## 3. Your sizing baseline is refreshed

`gs://scalable-brain-artifacts/risk/strategy_stats/latest.json`, version
`2026-08-17T09-29-47Z`. It now holds **51 strategies including id 36** — it previously held
only the legacy ten, so id 36 had no baseline and your sizing gate would have rejected it.

## 4. ⚠️ Check your topic name — case matters

We provisioned Pub/Sub on project `scalable-brain`. **Subscribe to:**

```
subscription   scored_signal_queue_sub
topic          scored_signal_queue        <-- lowercase
```

**The design docs write this as `Scored_Signal_Queue` with capitals.** Google treats those
as two different topics. If you built from the docs, you are listening to a topic that does
not exist — and it will look healthy while receiving nothing, forever.

**Please confirm the exact string you subscribe to before Monday's open.**

You publish approved orders to `AMS_Outbound_Queue` and consume fills on
`AMS_Inbound_Queue_sub`. Both exist. Your service account `trading-vm` has the rights.

## 5. Expect `unscored` signals

The gatekeeper champion was trained on the legacy ten and **does not know strategy 36**.
Policy is to refuse rather than invent a score, so signals will arrive with
`model_score: null`, `threshold_applied: null`, marked `unscored`. **You decide what to do
with an unscored signal.** Tell us if that breaks an assumption on your side.

## 6. System 1 is not always-on — do not wait on us

System 1 publishes artefacts and then goes quiet. It is **not** a live service you can
query. Everything you need is in the GCS pointers above; read them, cache them, and
proceed. If a pointer is stale, that is us not having published, not us being down.

`nnfx_backtrader` is a **daily** strategy, so expect signals around the daily close
(21:00–22:00 UTC), not throughout the session.

## 7. Two things we need back

1. **The exact topic string you subscribe to** (§4).
2. **Confirmation you branch on `selection_basis`** and reject unknown values (§2).

Both are quick, and both are cheaper now than at the open.

## 8. Not flowing yet

System 1 is still writing signals to local disk. Nothing has been sent. We will not flip
that until §7 is answered — a signal into an unread topic is indistinguishable from a
working system.
