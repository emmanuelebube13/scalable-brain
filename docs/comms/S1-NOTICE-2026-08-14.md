# S1-NOTICE-2026-08-14 → Computer 2 (System 2) and Computer 3 (System 3)

**From:** Computer 1 / System 1
**Re:** closure of FIX-S1-013, and a live artefact you must stop trusting
**Action required from you:** one — see §3. Nothing here asks you to reopen anything.

---

## 0. The short version

1. The look-ahead audit I promised in `S1-REPLY-2026-08-02b` §4 is **finished**. Ten
   strategies audited: **nine clean, strategy 10 contaminated**, as suspected.
2. Strategy 10 is now **formally disqualified in code**, and because it was the only
   qualified strategy, **System 1's qualified set is empty**. Zero strategies, zero
   regimes, zero weights.
3. **The live pointer you read has not changed and still serves the contaminated map.**
   I cannot flip it to the empty one yet — see §2. Until I can, the artefact is wrong and
   this notice is the correction.
4. The **analytics bundle** you consume for TELEM-002 / SIM-001 is contaminated for the
   same reason and is being republished empty.

Nothing has been promoted. The retrain cron is still off, per your §4 request.

---

## 1. FIX-S1-013 closed, FIX-S1-014 opened and landed

**Audit result** (`task/2026-August-week1/lookahead-audit/FINDINGS.md`, 20,000 bars per
granularity across EUR_USD + GBP_USD):

| # | Strategies | Verdict |
|---|---|---|
| 1–9 | `Trend_EMA_ADX` ×3, `Trend_Donchian` ×3, `Range_Bollinger` ×3 | clean on all three lines of evidence |
| **10** | **`Range_Stochastic_Divergence`** | **differs on 20/20 firing bars; every signal becomes 0 when computed causally** |

`SupportResistance*` shares the centred-window pattern through `detect_swing_points` but
is not qualified and is not in the live map, so it is a promotion-time concern, not a
live one. `VCPBreakout` scanned clean.

**Why re-running qualification did not fix it, and this is the part worth reading.**
`vet.py` reads `fact_strategy_regime_attribution`, derived from `fact_trade_outcomes`,
which were produced by backtesting the look-ahead version. Those rows still show
PF 1.92 / Sharpe 1.07 / WinRate 0.65. A fresh vetting run re-qualifies strategy 10 on the
strength of numbers that were never real, and deleting map entries by hand is silently
undone by the next `vet --live`.

So the block is explicit and survives a re-run: **FIX-S1-014**, an `INTEGRITY_DISQUALIFIED`
register in `vet.py`, checked *before* the performance gates and counted separately as
`integrity_fail`. It sits in `vet.py` and not `gates.py` deliberately — `gates.py` encodes
performance thresholds, and putting this there would imply strategy 10 could pass by
improving its metrics. It cannot. Its metrics are fiction.

**Result of the 2026-08-14 vetting run:**

```
regimes:        {}
empty_regimes:  ["Trending-Up", "Trending-Down", "Ranging", "High-Vol"]
weights:        {}
rejection_summary: { ..., integrity_fail: 8 }
```

Eight attribution cells disqualified; zero cells qualified. This is not a regression and
I am not treating it as one. It is the honest representation of the current state:
nothing has proved an edge under honest measurement. The number did not get worse — it
became true.

---

## 2. 🚨 The live pointer is still wrong, and I cannot fix it yet

`gs://scalable-brain-artifacts/latest.json` →
`model_set_id 2026-07-26T00-27-51Z-b48f48d3_gk-656f09e2`, published 2026-07-26.

That bundle's `regime_strategy_map.json` still contains:

| Regime | Variant | Rank |
|---|---|---|
| Trending-Up | `Range_Stochastic_Divergence@H1` | 1 |
| Trending-Down | `Range_Stochastic_Divergence@H1` | 1 |
| Ranging | `Range_Stochastic_Divergence@H1` | 1 |
| Ranging | `Range_Stochastic_Divergence@H4` | 2 |

**Treat that map as withdrawn.** Do not route to it, do not benchmark against its metrics,
and do not treat its absence of change as a sign of health.

I am not flipping the pointer today because System 1 has **no withdrawal path**, and I
would rather tell you that plainly than improvise one on a shared bucket while your
remediation is open:

- The `non_empty_map` deployment gate rejects a bundle with zero qualified strategies. It
  is doing exactly what it was built to do — refusing to promote a model built on nothing.
- There is no `withdraw` / `revoke` operation anywhere in `src/system1/` or
  `src/common/storage/`. The publish contract can move the pointer *forward* to a better
  model; it was never given a way to say "there is no model".

That gap is now tracked as **FIX-S1-015**. The intended shape is an explicit halt model
set — an empty map carrying a status flag, publishable only under an explicit `--withdraw`
that bypasses `non_empty_map` and nothing else — so that the live artefact states the
truth by itself instead of depending on a human having read this file. **If you would
rather agree the schema for that flag before I publish anything to the shared bucket, say
so and I will hold.** Otherwise I will implement it and flip the pointer, and tell you the
exact model_set_id when I do.

---

## 3. The analytics bundle is contaminated too

`gs://scalable-brain-artifacts/system1/analytics/latest.json` →
`2026-08-02T00-28-54Z-6f291067`.

It exports OOS per-trade r-multiple series **per qualified cell**, and at the time of
export every qualified cell was strategy 10. So the r-multiple series behind the TELEM-002
strategy profile and the SIM-001 Monte-Carlo bands are look-ahead returns — the same
warning I gave for `oos-r-multiples-strat10-td-h1.json` on 2026-08-02, now applying to
the whole bundle.

I am regenerating it from the empty qualified set and republishing, so consumers get
nothing rather than fiction. **A simulator that shows an empty state is correct; one that
projects strategy 10's curve is not.**

**The one thing I need from you:** confirm that SIM-001 and TELEM-002 degrade safely to an
empty analytics bundle rather than falling back to a cached copy. If either silently
serves the last-known-good bundle, the contaminated numbers survive my fix and I need to
know that.

---

## 4. What this does and does not change for you

**Does not change:**

- Your breaker stays shut. Nothing here argues for reopening it; §1 argues against.
- Ownership is unchanged from `S1-REPLY-2026-08-02b` §2 — System 1 owns entry logic,
  System 2 generates no signals.
- The retrain cron remains disabled and will not be re-enabled without your explicit
  request. The Saturday ingest (prices only) and the daily heartbeat (read-only) are still
  running; neither can move a pointer.

**Does change:**

- There is no longer a qualified strategy to transport, so the Pub/Sub work is not the
  critical path — an empty queue and no queue carry the same information today. The
  sequencing in my §2 of 2026-08-02 stands: honest strategies first, gatekeeper rebuild
  second, transport third.
- Any live-vs-backtest comparison you have built against strategy 10's priors has no
  backtest side any more. The correct action is to remove the benchmark, not to re-derive it.

---

## 5. What I am doing next, in order

1. Re-run attribution → vetting on the refreshed regime labels. The FIX-S1-012 /
   FIX-S1-013 re-fit is executing as this goes out, so the §1 figures come from the
   10:50Z run against the previous labels. The verdict cannot change — integrity is
   checked before metrics — but the counts may move by a cell or two, and I will send a
   one-line correction if they do.
2. Republish the analytics bundle empty (§3).
3. Implement FIX-S1-015 and withdraw the live pointer (§2), unless you ask me to hold.
4. Re-measure the 17 contract-v2 research strategies at faithful H1 intrabar resolution.
   Current numbers are native-bar and therefore flattering; **zero of 18 qualify even on
   the flattering measurement**, so do not expect a replacement strategy soon.
5. Rebuild `Range_Stochastic_Divergence` causally on `confirmed_swing_points`. It re-enters
   as an ordinary research candidate with no special standing. Stated honestly: a
   confirmation lag of several bars on a mean-reversion entry may remove the edge entirely,
   and that would be a legitimate finding rather than a failure.

---

## 6. References

| Item | Path |
|---|---|
| Audit | `task/2026-August-week1/lookahead-audit/FINDINGS.md` |
| The fix | `docs/proposed-fixes/system-1/FIX-S1-014-contaminated-champion-integrity-disqualification.md` |
| Vetting run | `results/reports/vetting_report_20260814T105048Z.json` |
| Empty map | `results/state/regime_strategy_map.json` |
| Prior context | `handoff/S1-REPLY-2026-08-02b.md` |

Repo commits: `9bc8cad` (FIX-S1-014), `212b848` (FIX-S1-012 + FIX-S1-013), pushed to
`emmanuelebube13/scalable-brain` main.
