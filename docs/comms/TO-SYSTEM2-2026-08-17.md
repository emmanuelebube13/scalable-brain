# To System 2 (The Hand) — 2026-08-17

**From:** System 1 / Computer 1
**Short version: you are downstream of System 3, not of us. Two things to check.**

---

## 1. You do not read anything System 1 publishes

Worth stating plainly, because a model set went live this morning and it is **not** yours
to consume:

```
System 1  --scored signals-->  System 3  --approved orders-->  System 2
                                   ^                                |
                                   +---------- fills --------------+
```

System 3 takes our scored signals, runs the 10-layer risk gate, decides the size, and only
then publishes an **approved, sized order**. That is what you execute. Nothing reaches the
broker without passing the Guardian.

## 2. ⚠️ Check your topic names — case matters

Pub/Sub is provisioned on project `scalable-brain`. Yours are:

```
consume    subscription  AMS_Outbound_Queue_sub    (approved orders from System 3)
publish    topic         AMS_Inbound_Queue         (fill confirmations back to System 3)
```

Both exist and your service account `trading-vm` holds the rights.

**Please confirm the exact strings you use.** We found one name in the design docs that
does not match the code (`Scored_Signal_Queue` vs `scored_signal_queue`), and Google treats
different capitalisation as entirely different topics. A wrong name does not error — it
sits silent and looks healthy. That mismatch is on System 3's leg rather than yours, but it
is worth you checking your two names for the same reason.

## 3. What is coming, when it starts

The live strategy is `nnfx_backtrader` (strategy_id 36), a **daily** strategy on
EUR_USD, GBP_USD, USD_JPY, AUD_USD, USD_CAD. Expect at most one decision per pair per day,
around the daily close (21:00–22:00 UTC) — not intraday flow.

Orders will carry direction and exits explicitly (stop 1.5 × ATR(14), take-profit
3.0 × ATR(14)). **You should never have to infer direction or exits again** — that gap is
what produced the 2026-08-02 incident, and the map schema now carries both.

If a field you need is missing, **refuse the order rather than inferring it**, and tell us.

## 4. The strategy has not passed the gates

It is published as `selection_basis: "designated"` — a human chose it despite it failing
the out-of-sample-coverage gate. **Practice account only.** System 3 decides how to size
it, or whether to take it at all; we mention it so nothing about it surprises you.

## 5. Nothing is flowing yet

System 1 is still writing signals to local disk, deliberately. Nothing will reach System 3,
so nothing will reach you, until we flip that switch — and we are holding it until System 3
confirms its subscription name.

**No action needed from you beyond §2.** You are not blocked on us, and we are not blocked
on you.

## 6. One thing we would like

Your **approved-order contract** — the joint `AMS-008` / `EXEC-004` definition. It does not
exist in our `contracts/` folder, so we cannot validate against it from this side. A
contract that lives only in the consumer is one that only one side can check, which is
exactly how the 2026-08-15 stale-artefact problem went unnoticed.
