Scalable Brain - PostgreSQL Connection Details
=============================================

Credentials are NOT stored here. They live in `.env` at the repo root, which is
git-ignored. See `.env.example` for the full set of keys and their meaning.

Host:     localhost
Port:     5432
Database: ForexBrainDB
Username: sa
Password: see DB_PASS in .env

Connect only through `src/common/db.py` (`get_engine()`), never by building a
connection string inline:

    python -c "from src.common.db import get_engine; get_engine().connect(); print('DB OK')"

History
-------
Until 2026-07-29 this file carried the live `sa` password in plaintext and was
tracked in git. That credential has been rotated and no longer authenticates.
See docs/proposed-fixes/cross-cutting/FIX-XC-003-db-password-committed-in-tracked-files.md
