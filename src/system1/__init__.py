"""System 1 — Model Building.

Additive task code for the System-1 model-building pipeline (MODEL-001 … MODEL-010).
Each subpackage maps to a MODEL task; existing layers are extended in place where a
task spec points at existing code (see orchestration/FOLDER_STRUCTURE.md).
"""
