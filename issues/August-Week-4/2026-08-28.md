# Issues — 2026-08-28

Found during the deep cleanup pass (CLEANUP-2026-08-28.md).

---

## ISSUE-1: `docs/strategy-notes.json` at the docs root

**File:** `docs/strategy-notes.json`

`docs/` is for prose; a JSON data file does not belong there. Unclear whether this is:
(a) hand-authored research data that belongs in `docs/research/`,
(b) machine-written output from a pipeline step that belongs in `results/`, or
(c) a config file for one of the `docs/frontend/` viewers.

**Action needed:** Determine the writer and reader. If no code writes/reads it, treat as
research data and move to `docs/research/`. If a pipeline step writes it, move to `results/`.

---

## ISSUE-2: `src/layer0/promotion/layer2_strategy_map.txt` — verify no reader

**File:** `src/layer0/promotion/layer2_strategy_map.txt`

This `.txt` file sits inside `src/`. STRUCTURE.md says `src/` is for Python that actually
executes. Could not confirm during the cleanup pass whether any module reads it by path.

**Action needed:** `grep -rn "layer2_strategy_map.txt" src/ shell/` — if nothing reads it,
move to `docs/design/` or delete.

---

## ISSUE-3: `.env.bak` files in `secrets/` need owner decision

**Files:** `secrets/.env.bak-20260817`, `secrets/.env.bak-20260822T172002Z`

Moved from root to `secrets/` (git-ignored). The 2026-08-17 backup is 1706 bytes vs current
`.env` 2064 bytes — it predates the 2026-08-22 update and may contain a credential not in
the current file. If so, it is the only copy of whatever was rotated.

**Action needed:** Owner to compare contents and confirm the current `.env` supersedes both,
then delete from `secrets/`.

---

## ISSUE-4: `archieved/` contains unpacked trees

**Path:** `archieved/`

STRUCTURE.md rule: "A `.zip` and its `.sha256`, nothing else." The 2026-08-16 cleanup found
unpacked trees (`layer4_executor/`, `layer5/`, `layer6_auditor/`, `layer7/`, `OtherSystems/`,
`v1-legacy-object/`, etc.) and deferred them because `CLAUDE.md` cites some by path.

**Action needed:** Either (a) update `CLAUDE.md` references to point at the zip instead, then
zip and SHA256 the unpacked trees, or (b) explicitly decide the rule does not apply to
`archieved/` and update STRUCTURE.md accordingly.

---

## ISSUE-5: `results/reports/` retention automation

**Current state:** The 2026-08-28 cleanup pass applied the retention policy manually
(kept last 7 ingest/dq, kept all others). Without automation this will require another
manual pass in ~6 days.

**Action needed:** Add a cleanup step to a cron script or `results/README.md` rotation note
so the next pass is automated: `ls results/reports/ingest_manifest_*.json | sort | head -n -7 | xargs rm -f`
(and equivalent for dq_gap_report). The 7-day window gives full coverage between weekly runs.
