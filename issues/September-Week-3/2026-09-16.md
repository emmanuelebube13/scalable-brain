# 2026-09-16

Logged during the owner-requested core verification pass (regime structure & artifacts /
strategy calculation & placement / cron & automation), three read-only review agents +
spot verification. Plan: `task/2026-September-week3/core-verification/PLAN.md`.

---

## The published map expires 2026-09-18T18:41Z and nothing automated republishes it

**Found while:** verifying the artifact structure System 2 downloads.
**Where:** backend model set `2026-09-11T19-47-36Z-e50032eb_gk-d614163c` (read via `build.load_model_set`); `src/scheduler/orchestrator.py` promote path; `MODEL_SET_AUTOPUBLISH` unset.
**What:** The map that actually routes (the backend bundle's) was built 2026-09-11T18:41Z with `expires_at_utc 2026-09-18T18:41:22Z`. The Sunday retrain renews only the *local* map (and the sub-pointers on a gated promotion); with `MODEL_SET_AUTOPUBLISH` unset the top-level manifest never moves, and the next Sunday window (09-20) is *after* expiry anyway. From Friday evening 09-18 `map_contract.routing_refusals` will refuse the expired map and the producer emits nothing until a human runs `publish_model_set`.
**Evidence:** published map header (`expires_at_utc`, `max_age_days 7.0`); `orchestrator.py:388/423-426`; local `results/state/regime_strategy_map.json` built 2026-09-14T16:21Z was never published (backend still serves the 09-11 set).
**Why it matters:** fail-closed is by design, but under the owner's 2026-09-16 24/7 decision a silent weekly blackout is the opposite of intent. Structural: the 2026-09-14 "retrain renews the map" decision renews a file routing does not read.
**Not done:** did not publish anything; did not check whether the 09-14 local map would pass the deployment gates.

## `s1_health.json` regime_map block reads the local map, not the backend — the Monday glance cannot see the gap above

**Found while:** same pass.
**Where:** `src/monitoring/publish_health.py:132-165` (reads `results/state/regime_strategy_map.json`).
**What:** The block the 2026-09-14 owner decision made the post-publish check reads the *local* map, while routing reads the *backend* map. It also renders no verdict (`routing_refusals()` is pure and never called here; a negative `expires_in_sec` must be eyeballed) and an absent map renders as `{}` rather than an alarm.
**Evidence:** contrast `publish_health.py:110-128`, which goes to the backend for `model_set` with a comment saying exactly why — the principle stops one block later.
**Why it matters:** the health file will report a fresh 09-14 map while the published one expires on 09-18. The one control built to catch stale maps watches the wrong file.
**Not done:** did not check what the dashboard renders from this block.

## Strategy 43 re-arms one pending setup as 15 distinct wire signals; no dedup layer catches it

**Found while:** reading `results/signals/*.ndjson` for 2026-09-10 → 09-16.
**Where:** `src/signals/build.py` (~:492 signal_id = uuid5 over `{sid}_{inst}_{gran}_{bar_ts}`); `reference_pullback_continuation` emits `buy_stop` with explicit `entry_price`.
**What:** 15 ledger rows 2026-09-14T09:00 → 09-16T17:00, EUR_USD H4, `proposed_entry` byte-identical (1.16564), only two distinct (sl, tp) pairs — one pending level re-armed each H4 close, minted as a fresh `signal_id` per bar. `signal_id` dedup, `bar_content_sha256`, and the per-run dedup all key on the bar, so all 15 published. System 3's Layer-R re-fire guard uses a 90-minute recent-approval window; these arrive 4 h apart, so each one clears it.
**Evidence:** ledger rows `a60b7f0c` … `503f5d17`; O-26 shows the same shape realised as double exposure with real losses.
**Why it matters:** with live money, one setup can be sized up to 15×. Distinct from O-23 (same id, different entry) — this is different ids, same trade idea. Owner question Q1 (is re-affirmation ever wanted?) governs both.
**Not done:** could not verify from this repo whether System 2 opened multiple positions off these.

## Computer 1's crontab runs in America/Halifax local time; every doc says UTC

**Found while:** cron/automation audit.
**Where:** `crontab` (no `CRON_TZ`); `timedatectl` → America/Halifax (ADT, −03:00); script headers, `shell/README.md`, `src/monitoring/job_runs.py` comments, `docs/critical/REPO_STATE.md` all label the schedules UTC.
**What:** Real firing is +3 h from documented, shifting to +4 h at the ADT→AST transition 2026-11-01. Intent-order (ingest → outcomes → stats → heartbeat) survives the offset; the documented times and the DST drift do not. The orchestrator's Sunday-00:00-UTC *trigger* is unaffected (evaluated in UTC by the hourly poll).
**Evidence:** `results/state/crontab.backup-20260907.txt` vs `timedatectl`.
**Why it matters:** freshness models and humans reason about these times; on 2026-11-01 every job silently moves an hour against the FX session.
**Not done:** did not change the crontab (owner-installed, hand-managed).

## 24/7 readiness gaps: unsupervised loop, unscheduled job-absence check, no alerting for Computer 1

**Found while:** cron/automation audit, against the owner's 2026-09-16 decision to run System 1 24/7.
**Where:** `src/signals/run.py:657-691` (bare `while True` loop, no supervisor/lock/timeout/job-record); `src/monitoring/job_runs.py` (absence detector — no scheduled caller); `work/scalablebrain-bridge/ops_watchdog.py:61` (`UNITS` excludes everything on Computer 1).
**What:** (a) the continuous producer mode lacks every protection the hourly cron wraps around `run --once` (`timeout 10m`, `flock`, `_job_record.sh`, health/ledger/model-card publishing) and would hang indefinitely on the 2026-09-09-style Pub/Sub stall; (b) the mechanism built to detect "a job stopped running" is itself never run; (c) no failure on Computer 1 pages anyone — the only alert channel (Telegram, via ops-watchdog on the VM) does not read `s1_health.json`. Also: `GOVERNANCE.md:252` and `shell/cron_signal_producer_daily.sh:4-13` still document the not-always-on design; no repo doc records the new decision. No Discord code exists anywhere; Telegram is the only built channel.
**Evidence:** file cites above; WO-06 (`task/2026-September-week2/emit-a-signal/WORK-ORDER-06.md`) for the stall.
**Why it matters:** "unforgiving to System 1" requires something to notice when System 1 dies. Today nothing does.
**Not done:** did not verify the VM's installed units/crontab (commands to run on each machine are in the plan).

## `vet.py` re-runs silently wipe owner-designated exits (escalation of the known empty-exits defect)

**Found while:** verifying the published artifact structure.
**Where:** `src/vetting/vet.py:461` (`"exits": {}` for every cell); `src/vetting/designate.py:299` writes real exits; `vet.py:444-459` re-attaches designation fields selectively — `exits` is not among them.
**What:** The known defect ("qualified strategies publish with empty exits") is worse: a `vet --live` rebuilds designated cells too, so exits an owner wrote via `designate` are erased on the next vetting run — which is what happened between the 2026-08-17 and current maps. All 8 published cells carry `exits: {}`. The contract can't catch it: `regime-map-contract.json` types `exits` as bare `object`, so `{}` validates.
**Evidence:** 2026-08-17 published map (4 cells with full exit specs) vs current published map (8/8 empty); grep shows no reader of the map's `exits` in `src/` (producer reads `intent.exits`), so blast radius today is a false promise to downstream consumers, not live behaviour.
**Why it matters:** any future consumer that trusts the field acts on an empty object; the designate CLI's `--exits` argument is currently a no-op one vet run later.
**Not done:** did not decide fix direction (populate from strategy intent vs delete the field from the contract).

## `publish_model_set` publishes `qualification_run_id: null` without complaint

**Found while:** same pass.
**Where:** `src/serializer/publish_model_set.py:385-397` (`_read_json(...) or {}` then `.get()`, no null-check).
**What:** The field exists precisely because two stale artefacts once agreed with each other (S2's 2026-08-15 incident), but the binding is written, never enforced — a bundle whose map lacks the id publishes with `qualification_run_id: null`. (Map-level enforcement exists separately in the vetting contract; the manifest-level check does not.)
**Why it matters:** the manifest is the one document System 2 reads; a null here defeats its purpose silently.
**Not done:** nothing — one-line guard, waiting on the plan.

## System-2-side contract gaps (verify on the running host, then comms)

**Found while:** cross-checking producer promises against `work/system2Executor/` (reference copy — may be stale vs deployed).
**Where / What:**
- `src/system2/artifact_sync/downloader.py:114-116` — a manifest with **no `status`** is treated as live ("not withdrawn"), the exact inversion of the agreed REJECT-on-unknown rule; also accepts `"active"`, a value System 1 never writes.
- `regime_mapping.py:19-38` — still the pre-FIX-S1-012 rank-based argmin mapping System 1 retired 2026-08-14; header cites the deleted `src/system1/regime/mapping.py` path. Applied by `live_regime.py` at any granularity with no H4 guard (mitigant: regime was demoted from the risk pipeline per d12 note).
- `regime_strategy_map.json` is downloaded, checksum-verified, and **read by no System 2 code** (grep: markdown only); `qualification_run_id` is likewise never parsed.
**Why it matters:** the withdrawal veto (the owner's kill switch) relies on S2 honouring `status`; today it fails open on absence.
**Not done:** none of this verified against the deployed host — local copies are reference only (CLAUDE.md). A comms message per `write-comms` is the vehicle, after host verification.

## Smaller findings, one line each

- **`structural.py`:** `build_structural_labels(granularity=...)` ignores its argument — vol window hardcoded 252 (lines 253/256); `get_vol_window()` (D1 252 / H4 1512 / H1 6048) has zero callers; the per-time-of-day groupby makes H4/H1 approximately right, but the signature advertises a control that does not exist. `build_structural.py:336` passes it believing it matters.
- **`src/gatekeeper/features.py:3`:** dead import of `build_structural_labels` — a loaded gun next to the "never recompute labels in a consumer" rule.
- **`hmm_regime.py:104-115`:** unverified whether the degenerate `acc=1.000/kappa=1.000` causal labels (fold-collapse defect, anchor since fixed) are still resident in `fact_market_regime_v2` and whether qualification run `21d6d29b` was attributed on them.
- **`GCP_scalablebrain/local_bucket_folder/latest.json`** is a stale mirror (2026-08-17 set) — the backend serves the 2026-09-11 set; do not read state from that folder.
- **Register row O-27 is stale:** `AUTHORITATIVE_ENGINE_FOR_VETTING` is now set (`attribute.py:148`, `position_engine_v2`), and the retrain cron IS installed — the row still says unset/not-installed. (Engine partitioning itself verified fine: filtered via `dim_strategy.engine`, `attribute.py:185` — `fact_trade_outcomes` needs no engine column.)
- **`shell/README.md`** lists 4 of the 7 installed cron jobs (missing retrain, strategy-stats, persist-outcomes).
- **T6 ATR case mismatch (O-12) re-confirmed live:** writer `engine_adapter.py:58` (`df["atr"]`) vs readers `strategy_base.py:321-379` (`df["ATR"]`); v1-evidence-path only; an active test (`test_position_engine.py:256-283`) documents and *tolerates* it.
