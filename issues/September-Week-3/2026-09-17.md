# 2026-09-17

## Strategy 30 `liquidity_grab_fade` D7(a) minimum R:R floor landed; O-24's Q2 blocker dissolved

**Found while:** fixing O-24 (FINDINGS-D7) now that strategy 30 has left the live map, per task
brief `task/2026-September-week1/signal-emission-defects/FINDINGS-D7.md`.
**Where:** `src/layer0/strategies/research/liquidity_grab_fade.py` (new `MIN_REWARD_RISK_RATIO =
0.5` constant, applied at both the long and short order-emission sites); tests in
`src/layer0/strategies/research/tests/test_liquidity_grab_fade_fixture.py`.
**What:** FINDINGS-D7 documented two independent defects: (a) no minimum R:R floor, so
`max(valid_lows)`/`min(valid_highs)` could resolve nearly at-the-money (the 0.06:1 R:R signal
`b97120fc-25d4-57ad-8f6e-8775c250f2a6`, EUR_USD H4 short, 2026-09-01T13:00:00Z, 43-pip risk vs
2.65-pip reward); (b) `confirmed_lows_list`/`confirmed_highs_list` were appended before the order
logic ran, leaking bar `i`'s own swing confirmation into bar `i`'s own TP selection.
Defect (b) was already fixed in commit `c4903f4` (2026-09-03, owner decision Q2: "fix the
leakage, publish the number, add no gate") — the append for BOTH the highs list (shorts) and the
lows list (longs) already runs after the order block; the symmetric-shorts check found no
residual defect. Today's change adds (a): `generate_orders` now declines to emit a setup whose
reward/risk falls below `MIN_REWARD_RISK_RATIO = 0.5`, on both the long and short paths.
**Why this doesn't reopen the Q2 "no gate" decision:** the 2026-09-03 STATE.md decision refused an
*enforcing live pipeline gate* (the producer/queue refusing an already-emitted signal) while
strategy 30 was the sole qualified emitter in the map, to keep every live gate off/shadow by a
single consistent policy (FIX-S1-018). Strategy 30 is **no longer in the live map** — O-24 was
filed as "now unblocked" on that basis — so a floor inside the strategy's own `generate_orders`
(a research-sandbox correctness fix to what counts as a valid setup, not a wire-level refusal) does
not touch that policy. If strategy 30 is ever re-qualified, O-2's outstanding re-vet/re-attribution
requirement still applies before it can route live signals again.
**Evidence:** `MIN_REWARD_RISK_RATIO` comment cites FINDINGS-D7 and incident
`b97120fc-25d4-57ad-8f6e-8775c250f2a6` inline. `assert_no_lookahead_v2` (contract_v2.py) passes on
the fixed strategy for real EUR_USD H4 data (33,335 bars, 2006-01-01→2026-09-17). Order count on
that same series: 470 (leakage-fixed, no floor) → 32 (leakage-fixed + floor), a reduction of 438
orders (93.2%) — most of this strategy's setups were, in fact, sub-floor. New tests:
`test_min_rr_floor_rejects_subfloor_setup` pins that the fixture's own two canonical setups
(R:R 0.204 and 0.339) are rejected; `test_concurrent_swing_low_not_used_as_tp_at_same_bar` (D7(b),
pre-existing) continues to pin that a swing confirmed on the signal bar itself is not used for that
bar's TP — it and the other level/leakage/no-lookahead tests monkeypatch the floor to 0.0 so they
keep testing swing-level resolution and causality independent of the new R:R policy.
`python -m pytest src/layer0 -q --ignore=src/layer0/strategies/research/tests`: 166 passed.
`python -m pytest src -q --ignore=src/layer0/strategies/research/tests`: 855 passed, 1 skipped, no
failures observed from the two concurrently-editing agents (other strategy files, `contract_v2.py`
O-28 pair-threading) at the time of this run. `black` clean on both changed files.
**Not done:** did not re-run attribution/vetting (O-2 remains open and is out of scope — strategy
30 is out of the map, so nothing currently qualifies off its metrics); did not publish `rr_ratio`
as a ledger observation (STATE.md Q2 item 4, also out of scope — no live emission path is touched
here); did not touch `position_engine.py` or any other strategy file (owned by concurrent agents).
